# Integration test environment defaults
# Used by docker-compose.yml and CI pipeline
# Override any variable by exporting it before running make test-local

# MySQL
DB_ROOT_PASSWORD=testroot
DB_DATABASE=saas
DB_USERNAME=test
DB_PASSWORD=test

# JWT
JWT_SECRET=test-secret-key-for-integration-tests

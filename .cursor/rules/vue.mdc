---
description: 
globs: 
alwaysApply: true
---
1.前端文件的文件目录是 /admin/views/admin和/admin/views/shop
2.用中文和我对话

1. 代码风格
必须使用 ESLint + Prettier 统一代码风格。
组件文件统一使用 .vue 后缀，采用 单文件组件（SFC）格式。
变量、方法、组件名统一使用小驼峰（camelCase），组件文件名用大驼峰（PascalCase）。
必须使用 中文注释，注释要准确描述代码意图。
2. 组件开发
新组件必须使用 <script setup> 语法，禁止使用 Vue2 的 Options API。
组件 props、emits 必须使用 defineProps、defineEmits 显式声明类型。
组件必须有明确的 props 类型和默认值。
组件必须拆分为小而精的功能单元，禁止大杂烩。
组件必须有必要的单元测试或手动测试用例。
3. 状态管理
推荐使用 Pinia 或 Vuex4，禁止直接跨组件传递数据。
业务相关状态优先放在页面组件内，跨页面/全局状态才放到 store。
4. API 调用
所有 API 调用必须通过统一的 api 目录下的模块进行，禁止在组件内直接写 axios/fetch。
API 返回数据必须做类型校验和异常处理。
5. UI 规范
必须使用 Element Plus 组件库，禁止混用其他 UI 库。
表单校验规则必须写在 rules 变量中，且与表单字段一一对应。
所有弹窗、表单、按钮等必须有 loading 状态，防止重复提交。
所有表单、弹窗必须支持国际化（i18n）。
6. 国际化
文本内容必须通过 $t('xxx') 实现国际化，禁止写死中文。
组件/页面新增的文案必须同步到语言包。
7. 目录结构
页面组件放在 views/，通用组件放在 components/，api 放在 api/。
业务模块必须有独立目录，禁止所有文件堆在一起。
8. 代码提交
必须写清晰的 commit message，推荐使用英文+中文描述。
禁止提交未通过 lint 检查的代码。
禁止提交调试代码（如 console.log）。
9. 其他
禁止直接操作 DOM，必须通过 Vue 的响应式系统。
禁止在模板中写复杂表达式，复杂逻辑必须放到 setup 里。
禁止在组件外部直接修改 props。
10. 代码从vue2改为vue3时
使用api的时候 从.then .catch 改为 try{} catch。
proxy从vue引入getCurrentInstance, this.$filter.isAuth 替换成proxy.$filter.isAuth。
国际化（i18n）直接使用$t('')包住中文就可以。
国际化（i18n）script 标签里面的也可以直接使用$t(''),this.$t('')无须proxy.$t('')。
script 标签里面的方法，函数吗，变量，需要加分号结束。
el-form 表单的例如ref="form"的时候，需要改为ref="formRef" 这样不会和他的数据form冲突。

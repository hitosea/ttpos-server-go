---
alwaysApply: false
description: 打印模块开发规范（Main 模块）
---

# 打印模块开发规范

> 适用于 `main/app/printer/` 模块的打印功能开发

---

## ⚡ 核心规则

### 模板类型

| 类型 | 位置 | 说明 |
|------|------|------|
| **标准模板** | `printer/template/` | 硬编码，按打印机型号分 6 个文件 |
| **自定义模板** | `printer/pkg/template/` | JSON 配置驱动，用户可自定义 |

### 打印机类型

| 后缀 | 说明 | 示例 |
|------|------|------|
| `_img.go` | 通用图像 | `statement_order_img.go` |
| `_img_58mm.go` | 58mm 纸张 | `statement_order_img_58mm.go` |
| `_compax.go` | Compax 打印机 | `statement_order_compax.go` |
| `_codesoft.go` | Codesoft 打印机 | `statement_order_codesoft.go` |
| `_xprinter.go` | XPrinter 打印机 | `statement_order_xprinter.go` |
| `_sunmi.go` | Sunmi 打印机 | `statement_order_sunmi.go` |
| `_img_custom.go` | 自定义模板 | `statement_order_img_custom.go` |

---

## 🔄 新增字段流程

### 必须同步修改的 7 类文件

```
1. 数据结构（1 个）
   └── printer/template_struct/order.go

2. 标准模板（6 个）
   ├── printer/template/statement_order_img.go
   ├── printer/template/statement_order_img_58mm.go
   ├── printer/template/statement_order_compax.go
   ├── printer/template/statement_order_codesoft.go
   ├── printer/template/statement_order_xprinter.go
   └── printer/template/statement_order_sunmi.go

3. 自定义模板（5 个）
   ├── printer/pkg/template/statement_order_tmp.json
   ├── printer/pkg/template/statement_order_data.json
   ├── printer/pkg/template/statement_pre_tmp.json
   ├── printer/pkg/template/statement_pre_data.json
   └── printer/template/statement_order_img_custom.go
```

### 步骤概览

1. **数据结构**: 在 `order.go` 中添加字段
2. **标准模板**: 在 6 个模板文件中添加渲染逻辑（保持位置一致）
3. **JSON 配置**: 在 `*_tmp.json` 中添加配置块（包含所有 9 种语言）
4. **示例数据**: 在 `*_data.json` 中添加示例值
5. **数据填充**: 在 `*_img_custom.go` 中填充数据

---

## ✅ 关键约束

### 字段位置一致性
- ✅ 所有 6 个标准模板必须在相同位置添加字段
- ❌ 不同模板字段顺序不一致会导致混乱

### 多语言完整性
- ✅ JSON 配置必须包含所有 9 种语言：`zh`, `en`, `th`, `ja`, `ko`, `my`, `tr`, `sv`, `de`, `zhtw`
- ❌ 缺少任何语言翻译会导致显示异常

### 条件显示规则
- ✅ 金额字段使用 `> 0` 判断（避免显示 0 或负数）
- ✅ 标准模板：`if amount > 0 { ... }`
- ✅ JSON 模板：`"operator": "not_empty"` 或 `"operator": "gt"`

### 代码规范
- ✅ 使用 `t.base.Translate("字段名")` 支持多语言
- ✅ 使用 `t.base.GetPriceAndUnit(amount)` 格式化金额
- ✅ 金额类型使用 `float64`
- ✅ JSON 修改后必须通过 `jq empty` 验证

---

## ⚠️ 常见错误

### 1. 字段顺序不一致
❌ 不同打印机模板中字段顺序不同  
✅ 所有模板保持相同的字段顺序

### 2. 多语言翻译不完整
❌ JSON 配置只有中英文翻译  
✅ 包含所有 9 种语言的翻译

### 3. JSON 格式错误
❌ 直接提交未验证的 JSON  
✅ 使用 `jq empty *.json` 验证格式

### 4. 自定义模板数据未填充
❌ 只修改结构体，忘记在 `*_img_custom.go` 中填充  
✅ 同步更新数据结构和数据填充代码

### 5. 条件判断错误
❌ 使用 `!= 0` 会显示负数  
✅ 使用 `> 0` 只显示正数

---

## 📝 验证清单

完成修改后逐项检查：

- [ ] **数据结构**: `order.go` 添加字段（含 `json` tag 和注释）
- [ ] **6 个标准模板**: 字段位置一致，条件判断正确
- [ ] **JSON 配置**: 包含所有 9 种语言，格式正确
- [ ] **示例数据**: 添加测试值
- [ ] **自定义模板**: 数据填充代码已添加
- [ ] **代码格式**: `go fmt` 和 `go vet` 通过
- [ ] **JSON 验证**: `jq empty` 通过

---

## 📖 详细文档

- **完整开发指南**: [打印模块开发详细指南](../../docs/human/guides/printer-development-guide.md)
- **代码示例**: 见详细指南中的"代码示例"章节
- **排查指南**: 见详细指南中的"排查指南"章节
- **模板使用文档**: `main/app/printer/pkg/template/README_STATEMENT_ORDER.md`

---

## 🔍 快速参考

### 金额格式化
```go
t.base.GetPriceAndUnit(amount)  // 输出: "¥50.00"
```

### 多语言翻译
```go
t.base.Translate("活动抵扣")  // 自动翻译
```

### 条件判断
```go
if saleOrder.ActivityAmount > 0 {  // 只显示正数
    img.AppendText(...)
}
```

### JSON 验证
```bash
jq empty main/app/printer/pkg/template/*.json
```

---

**最后更新**: 2025-11-25  
**维护者**: TTPOS Team

---
alwaysApply: false
description: 项目结构快速定位
---

# 项目结构

3 个主要模块 + 基础设施

---

## 快速定位

| 我要找...                | 路径                              |
| ------------------------ | --------------------------------- |
| **Go Main 核心业务**     | `main/app/`                       |
| **PHP 管理后台**         | `admin/app/admin/`                |
| **PHP 店铺后台**         | `admin/app/shop/`                 |
| **Go BMP 微服务**        | `ttpos-bmp/app/ttpos-*/`          |
| **前端管理后台**         | `admin/views/admin/`              |
| **前端店铺后台**         | `admin/views/shop/`               |
| **数据库迁移**           | `admin/database/migrations/`      |
| **Docker 配置**          | `docker/`                         |
| **项目文档**             | `docs/`                           |
| **API 定义**             | `ttpos-api/`                      |
| **工具脚本**             | `ttpos-scripts/`, `scripts/`      |

---

## 3 个主要模块

### 1. Main 模块（Go + Gin）

**核心业务服务**

```
main/app/
├── api/              # API 层（路由处理）
├── service/          # 服务层（业务逻辑）
├── repository/       # 仓储层（数据访问）
├── model/            # 模型层（数据结构）
├── dto/              # 数据传输对象
│   ├── req/          # 请求参数
│   └── resp/         # 响应数据
├── constant/         # 常量定义
├── errors/           # 错误处理
└── event/            # 事件处理
```

### 2. Admin 模块（PHP + ThinkPHP）

**管理后台和前端**

```
admin/
├── app/
│   ├── admin/              # 管理后台
│   │   ├── controller/     # 控制器
│   │   ├── model/          # 模型
│   │   ├── service/        # 服务
│   │   └── validate/       # 验证器
│   ├── shop/               # 店铺后台（同上）
│   └── common/             # 共享代码
├── config/                 # 配置
├── database/
│   ├── migrations/         # 数据库迁移
│   └── seeds/              # 种子文件
└── views/                  # 前端代码（Vue3）
    ├── admin/              # 管理后台前端
    └── shop/               # 店铺后台前端
```

### 3. BMP 模块（Go + GoFrame）

**业务中台微服务**

```
ttpos-bmp/app/
├── ttpos-erp/          # ERP 服务
├── ttpos-manager/      # 管理服务
├── ttpos-message/      # 消息服务
├── ttpos-shop/         # 店铺服务
├── ttpos-takeout/      # 外卖服务
└── ttpos-websocket/    # WebSocket 服务

每个服务结构：
ttpos-*/
├── api/                    # API 接口定义（protobuf）
├── internal/
│   ├── controller/
│   │   ├── http/           # HTTP 控制器
│   │   └── rpc/            # gRPC 控制器
│   ├── logic/              # 业务逻辑
│   ├── dao/                # 数据访问（自动生成 ❌ 禁止修改）
│   └── model/
│       ├── entity/         # 数据实体（自动生成 ❌ 禁止修改）
│       ├── do/             # 数据对象（自动生成 ❌ 禁止修改）
│       └── dto/            # 数据传输对象（手动编写 ✅）
└── manifest/
    ├── config/             # 配置文件
    ├── protobuf/           # protobuf 定义
    └── sql/                # 数据库迁移
```

---

## 🔌 服务通信

### 内部通信
```
Main ←→ BMP-ERP (gRPC)
Main ←→ BMP-Message (gRPC)
Main ←→ BMP-Takeout (gRPC)
Admin ←→ Main (HTTP API)
Frontend ←→ Admin (HTTP API)
Frontend ←→ Main (HTTP API)
```

### 事件通信
- **内存事件总线**: Main模块内部（同步/异步）
- **RocketMQ**: 跨服务异步通信
- **WebSocket**: 实时推送

---

## 3 层依赖架构

```
API 层 (Controller/API)
  ↓ 依赖
业务层 (Service/Logic)
  ↓ 依赖
数据层 (Repository/DAO)
```

### 依赖规则

- ✅ 上层可依赖下层
- ❌ 禁止下层依赖上层
- ❌ 禁止跨层调用
- ❌ 禁止循环依赖

**特殊规则**：
- ✅ Service 可以依赖其他 Service
- ❌ Service 不能依赖 Repository
- ❌ Repository 不能持有 DBManager（只持有 db）

---

## 基础设施

```
docker/                 # Docker 配置
├── mysql/              # MySQL 容器
├── redis/              # Redis 集群
├── nginx/              # Nginx 反向代理
├── php/                # PHP-FPM
└── golang/             # Go 应用

docs/                   # 项目文档
├── agent/              # 🤖 Agent 专用
│   ├── workflows/      # 工作流清单
│   ├── templates/      # 模板
│   └── graphiti/       # Graphiti Episode 草稿
├── human/              # 👤 人类专用
│   ├── guides/         # 开发指南
│   ├── architecture/   # 架构设计
│   └── business/       # 业务知识
├── shared/             # 📚 共用资源
│   ├── specs/          # 功能规格
│   ├── api/            # API 文档
│   └── troubleshooting/# 问题排查
└── team/               # 👥 团队协作
    ├── proposals/      # 需求提案
    ├── activities/     # 活动日志
    └── reports/        # 评估与复盘

.cursor/                # Cursor AI 配置
├── rules/              # 开发规范
├── commands/           # Cursor 指令
└── checklists/         # 检查清单
```

---

## 禁止依赖

| 禁止                             | 原因                     |
| -------------------------------- | ------------------------ |
| Service → Repository             | Service 只能依赖 Service |
| Repository → Service             | 不能向上依赖             |
| Controller → Repository          | 必须通过 Service         |
| ttpos-bmp 修改 dao/entity/do/service | 自动生成，修改会被覆盖   |
| API 层包含业务逻辑               | 职责单一                 |
| Repository 持有 DBManager        | 只能持有 db 实例         |

---

## 常用命令

```bash
# 开发环境启动
docker-compose -f docker-compose.dev.yml up -d

# Go Main 服务
cd main && go run main.go

# PHP 服务
cd admin && php think run

# ttpos-bmp 服务
cd ttpos-bmp && make run

# 数据库迁移
cd admin && php think migrate:run

# 运行测试
cd main && go test ./...
cd admin && php think unit

# 代码生成（ttpos-bmp）
cd ttpos-bmp/app/ttpos-erp && make dao
```

---

## Web 平台兼容

不涉及（纯后端项目）

---

## 相关资源

- [项目总览](./project-overview.mdc) - 详细架构说明
- [Go Main 规范](./go-main.mdc) - Go Main 开发规范
- [Go BMP 规范](./go-bmp.mdc) - Go BMP 开发规范
- [PHP 规范](./php.mdc) - PHP 开发规范
- `docs/human/architecture/overview.md` - 架构详细文档
- `docs/human/architecture/modules.md` - 模块关系详解

---

**最后更新**: 2025-11-17  
**维护者**: TTPOS Team

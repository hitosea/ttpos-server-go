---
alwaysApply: false
description: Spec 命名和结构规范
---

# Spec 规范


---

## 命名格式

```
{level}-{module}-{feature}
```

**参数
- `{level}`: `story` (用户故事) | `task` (技术任务)
- `{module}`: 业务模块（order, member, product, shop, admin, bmp...）
- `{feature}`: kebab-case

**示例
```
story-order-quick-payment         # 订单模块快捷支付
task-shop-report-export           # 店铺模块报表导出
story-member-sync                 # 会员模块同步功能
task-all-database-migration       # 全模块数据库迁移
```


---

## 最小细粒度原则 

### 核心原则

**按模块垂直拆分 Spec**

```yaml
❌ 错误:
story-order-member-product-sync  # 跨3个模块，难以控制SP

✅ 正确:
story-order-sync    # 订单模块，SP3
story-member-sync   # 会员模块，SP3
story-product-sync  # 商品模块，SP3
```

### 为什么

1. **独立交付 每个模块可独立开发、测试、部署
2. **并行开发 不同团队可同时开发
3. **SP 可控 单模块 Spec 的 SP 更容易 ≤5
4. **职责清晰 每个 Spec 有明确的负责团队


---

## 跨模块场景处理

### 场景 A: 相似功能，不同实现

**拆分为多个 Spec
```
story-order-status-sync
story-member-status-sync
story-product-status-sync
```

### 场景 B: 完全相同功能和实现

**使用 all
```
story-all-connectivity-indicator      # 共享代码
```

### 场景 C: 纯后端功能

**使用 task-all
```
task-all-api-refactor                 # API 重构
task-all-database-migration          # 数据库迁移
```


---

## Spec 目录结构

```
docs/shared/specs/{level}-{module}-{feature}/
├── requirements.md    # 需求规格说明
├── design.md          # 技术设计文档
└── tasks.md           # 实施任务分解
```

### 文件模板

- `docs/agent/templates/requirements-template.md`
- `docs/agent/templates/design-template.md`
- `docs/agent/templates/tasks-template.md`

---

## 与 Scrum Story Point 的关系

### 黄金规则

**只有 SP ≤ 5 的需求才能进入 Sprint**

### SP 评估流程

1. 技术复杂度评估
2. 功能复杂度评估
3. 因素加成
4. 团队评审

### 如果 SP > 5

**必须拆分** → 按最小细粒度拆分


---

## Spec 与 Proposal 的关联

### 关联机制

```
/spec-propose quick-payment → 需求评审 → /spec-create → 产品审核 → /spec-design
                                         ↓               ↓            ↓
                                   创建 requirements   审核通过    创建 design + tasks
```

### 双向链接

- Proposal → Spec
- Spec → Proposal


---

## 创建 Spec（两阶段流程）

### Phase 1: 创建需求文档

```bash
/spec-create story-order-quick-payment
```

**功能
- ✅ 创建 requirements.md
- ✅ 自动填充基本信息
- ✅ 智能关联 Proposal
- ✅ 初始化审核状态为「待审核」

**输出
```
docs/shared/specs/active/story-order-quick-payment/
└── requirements.md  # 审核状态: 待审核
```

### Phase 2: 创建设计文档（产品审核通过后）

```bash
/spec-design story-order-quick-payment
```

**前置条件
- requirements.md 存在
- 审核状态为「已通过」

**功能
- ✅ 检查审核状态
- ✅ 创建 design.md
- ✅ 创建 tasks.md

**输出
```
docs/shared/specs/active/story-order-quick-payment/
├── requirements.md  # 审核状态: 已通过
├── design.md        # 技术设计
└── tasks.md         # 任务分解
```


---

## 命名规范清单

### ✅ 允许

- 单个单词: `payment`, `category`, `report`
- 多个单词: `quick-payment`, `category-display`
- 使用 `all`: `story-all-dark-mode`

### ❌ 禁止

- 使用数字代替: ~~`feature1`~~ → `quick-payment`
- 使用缩写: ~~`cat-disp`~~ → `category-display`
- 过长名称: ~~`product-category-display-with-search-and-filter`~~ → 拆分

---

## 完整示例

参考: `docs/shared/specs/example-story-order-quick-payment/`

包含:
- ✅ 标准目录结构
- ✅ 完整 requirements.md
- ✅ 完整 design.md
- ✅ 完整 tasks.md
- ✅ Proposal 关联示例

---

## 相关文档

### 核心规范

- 详细指南 `docs/shared/specs/README.md` 
- SP 评估 `.cursor/rules/scrum_story_point.mdc`
- 需求管理 `docs/agent/workflows/requirement/management.md`
- Proposal 关联 `docs/agent/workflows/requirement/linking.md`

### 指令集

- 🔧 `/spec-propose` - 创建需求提案
- 🔧 `/spec-create` - 创建需求文档（requirements.md）
- 🔧 `/spec-design` - 创建设计文档（design.md + tasks.md）
- 🔧 `/check-tasks` - 检查任务进度

---

# Go Main 模块开发规范

> 本规范适用于 `main/` 目录下的 Go 代码开发

## 📋 目录

1. [项目结构](#项目结构)
2. [命名规范](#命名规范)
3. [代码风格](#代码风格)
4. [服务层设计](#服务层设计)
5. [Repository层设计](#repository层设计)
6. [错误处理](#错误处理)
7. [API响应规范](#api响应规范)
8. [事件总线使用](#事件总线使用)
9. [并发控制](#并发控制)
10. [性能优化](#性能优化)
11. [多语言使用规范](#多语言使用规范)

## 项目结构

```
main/
├── app/
│   ├── api/v1/           # API控制器
│   ├── service/          # 业务逻辑层
│   ├── repository/       # 数据访问层
│   ├── model/            # 数据模型
│   ├── dto/              # 数据传输对象
│   │   ├── req/          # 请求参数
│   │   └── resp/         # 响应数据
│   ├── constant/         # 常量定义
│   ├── errors/           # 错误定义
│   └── event/            # 事件处理器
├── pkg/                  # 基础设施包
│   ├── database/         # 数据库管理
│   ├── cache/            # 缓存
│   ├── eventbus/         # 事件总线
│   ├── lock/             # 并发锁
│   ├── logger/           # 日志
│   └── utils/            # 工具类
├── middleware/           # 中间件
├── router/               # 路由
└── config/               # 配置
```

## 命名规范

### 1. 结构体命名

✅ **正确**: 除了ID大写，其他用驼峰命名
```go
type Staff struct {
    StaffId   uint64  // ID字段大写
    StaffName string  // 其他字段驼峰命名
    CompanyId uint64
}
```

❌ **错误**: 不要全小写或下划线
```go
type staff struct {
    staff_id   uint64
    staff_name string
}
```

### 2. URL命名规范

✅ **正确**: 使用snake_case（蛇形命名）
```go
"/api/v1/passport/server_public_key"
"/api/v1/order/cart_info"
```

❌ **错误**: 不要使用kebab-case（烤串命名）
```go
"/api/v1/passport/server-public-key"
"/api/v1/order/cart-info"
```

### 3. 包名和文件名

✅ **正确**: 使用snake_case
```go
package member_service
// 文件名：member_service.go
```

❌ **错误**: 不要用驼峰
```go
package memberService
// 文件名：memberService.go
```

### 4. 接口和实现命名

✅ **正确**: 接口以I开头，实现以Impl结尾
```go
// 接口定义
type IProductCategoryRepo interface {
    CreateProductCategory(category model.ProductCategory) (uint64, error)
}

// 实现
type ProductCategoryRepoImpl struct {
    db *gorm.DB
}

// Repository简写成Repo，Service简写成Srv
type ICashierSrv interface {}
type CashierSrvImpl struct {}
```

## 代码风格

### 1. 包导入顺序

✅ **正确**: 自带包-第三方包-项目包，用空行分隔
```go
import (
    // 标准库
    "context"
    "fmt"
    "time"
    
    // 第三方包
    "github.com/gin-gonic/gin"
    "gorm.io/gorm"
    
    // 项目包
    "ttpos-server-go/app/model"
    "ttpos-server-go/app/service"
    "ttpos-server-go/pkg/context"
)
```

### 2. 注释规范

✅ **所有注释使用中文**
```go
// CreateOrder 创建订单
// 参数：ctx 上下文，req 创建订单请求
// 返回：订单响应，错误信息
func (s *OrderService) CreateOrder(ctx context.Context, req req.CreateOrderReq) (*resp.OrderResp, error) {
    // 验证请求参数
    if err := req.Validate(); err != nil {
        return nil, errors.WithMessage(err)
    }
    
    // 创建订单逻辑
    order := model.Order{
        ProductId: req.ProductId, // 商品ID
        Quantity:  req.Quantity,  // 数量
        Price:     req.Price,     // 价格
    }
    
    return &resp.OrderResp{}, nil
}
```

## 服务层设计

### 1. 接口定义和依赖注入

✅ **正确的服务实现模式**
```go
// 服务接口定义
type IOrderSrv interface {
    CreateInstantOrder(ctx context.Context) (resp.CreateInstantOrderResp, error)
    GetOrderLists(ctx context.Context, req req.OrderListReq) (resp.OrderListPaginationResp, error)
}

// 服务实现
type orderSrv struct {
    dbm              *database.DBManager
    localeSrv        ILocaleSrv
    settingSrv       setting.ISrv
    paymentMethodSrv IPaymentMethodSrv
}

// 构造函数
func NewOrderSrv(dbm *database.DBManager, localeSrv ILocaleSrv, settingSrv setting.ISrv) IOrderSrv {
    return NewOrderSrvImpl(dbm, localeSrv, settingSrv)
}

func NewOrderSrvImpl(dbm *database.DBManager, localeSrv ILocaleSrv, settingSrv setting.ISrv) IOrderSrv {
    return &orderSrv{
        dbm:        dbm,
        localeSrv:  localeSrv,
        settingSrv: settingSrv,
    }
}
```

### 2. 服务依赖规范

✅ **正确**: 服务A依赖服务B通过参数传递
```go
type orderSrv struct {
    memberSrv    IMemberSrv      // 通过接口依赖其他服务
    paymentSrv   IPaymentSrv
}
```

❌ **错误**: 不要直接依赖其他服务的Repository
```go
type orderSrv struct {
    memberRepo   IMemberRepo     // 不要这样做
}
```

## Repository层设计

### 1. Repository接口定义

✅ **正确的Repository模式**
```go
// Repository接口
type IProductCategoryRepo interface {
    CreateProductCategory(productCategory model.ProductCategory) (uint64, error)
    GetProductCategory(opts ...DBOption) (model.ProductCategory, error)
}

// Repository实现
type ProductCategoryRepoImpl struct {
    db *gorm.DB  // 只能传入db实例，不能传入dbm实例
}

func NewProductCategoryRepo(db *gorm.DB) IProductCategoryRepo {
    return NewProductCategoryRepoImpl(db)
}
```

### 2. 选项模式

✅ **使用选项模式进行查询**
```go
type IOrderRepo interface {
    WhereStatus(status uint8) DBOption
    WhereUuid(uuid uint64) DBOption
    GetOrder(opts ...DBOption) (model.Order, error)
}

// 使用示例
order, err := orderRepo.GetOrder(
    orderRepo.WhereUuid(orderUuid),
    orderRepo.WhereStatus(constant.OrderStatusActive),
)
```

## 错误处理

### 1. 不使用panic

✅ **正确**: 返回错误
```go
func GetUser(id uint64) (*User, error) {
    if id == 0 {
        return nil, errors.New("用户ID不能为空")
    }
    // 业务逻辑
    return user, nil
}
```

❌ **错误**: 不要使用panic
```go
func GetUser(id uint64) *User {
    if id == 0 {
        panic("用户ID不能为空")  // 不要这样做
    }
    return user
}
```

### 2. 错误包装

✅ **使用自定义错误包装**
```go
import "ttpos-server-go/app/errors"

func GetUser(id uint64) (*User, error) {
    user, err := repo.FindUser(id)
    if err != nil {
        return nil, errors.WithMessage(err, "查询用户失败")
    }
    return user, nil
}
```

### 3. 验证错误信息国际化

✅ **定义错误消息映射**
```go
var AddMemberReqMessage = map[string]string{
    "level_uuid.required": "会员等级不存在",
    "phone.max":           "手机号不能超过20个字符", 
    "nickname.max":        "昵称不能超过50个字符",
}

type AddMemberReq struct {
    Nickname     string `json:"nickname" binding:"omitempty,max=50"`
    Phone        string `json:"phone" binding:"required,max=20"`
    LevelUuid    uint64 `json:"level_uuid" binding:"required"`
}
```

## API响应规范

### 1. 统一响应格式

✅ **正确的响应格式**
```json
{
  "code": 1,
  "message": "success", 
  "data": {
    "list": [{"foo": "bar"}],
    "options": {
      "list": [{"key": "k1", "value": "v1"}]
    },
    "meta": {
      "page_no": 1,
      "page_size": 20, 
      "total": 100
    }
  }
}
```

❌ **错误**: data不能返回null或数组
```json
{
  "code": 1,
  "message": "success",
  "data": null  // 错误
}

{
  "code": 1, 
  "message": "success",
  "data": []  // 错误
}
```

### 2. 分页信息规范

✅ **分页信息统一放在meta中**
```go
type Response struct {
    Data struct {
        List []interface{} `json:"list"`
        Meta dto.PageResponse `json:"meta"`
    } `json:"data"`
}

type PageResponse struct {
    PageNo   int   `json:"page_no"`   // 当前页码
    PageSize int   `json:"page_size"` // 每页大小
    Total    int64 `json:"total"`     // 总数
}
```

## 事件总线使用

### 1. 定义事件

✅ **在pkg/eventbus/event目录下定义事件**
```go
// 文件：pkg/eventbus/event/order_created_event.go

const EventOrderCreated EventName = "Event_Order_Created"

type OrderCreatedPayload struct {
    BasePayload
    OrderId     uint64  `json:"order_id"`
    OrderAmount float64 `json:"order_amount"`
}

type OrderCreatedHandler func(msg OrderCreatedPayload)

func (system *SystemEventBus) PublishOrderCreatedEvent(msg OrderCreatedPayload) {
    system.bus.Publish(eventbus.Event{Name: string(EventOrderCreated), Payload: msg})
}

func (system *SystemEventBus) SubscribeOrderCreatedEvent(handler OrderCreatedHandler) {
    system.bus.Subscribe(string(EventOrderCreated), func(event eventbus.Event) {
        msg := event.Payload.(OrderCreatedPayload)
        handler(msg)
    })
}
```

### 2. 发布事件

✅ **在服务中发布事件**
```go
func (s *orderSrv) CreateOrder(ctx context.Context) error {
    // 业务逻辑
    
    // 发布事件
    go func() {
        event.NewSystemBus().PublishOrderCreatedEvent(event.OrderCreatedPayload{
            BasePayload: event.BasePayload{
                Ctx:          ctx,
                CompanyUuid:  ctx.GetCompanyUuid(),
                SaleBillUuid: saleBillUuid,
            },
            OrderId:     orderId,
            OrderAmount: amount,
        })
    }()
    
    return nil
}
```

### 3. 订阅事件

✅ **在app/event目录下创建事件处理器**
```go
// 文件：app/event/order_created_event_handler.go

func InitOrderCreatedEventHandler() {
    event.NewSystemBus().SubscribeOrderCreatedEvent(func(msg event.OrderCreatedPayload) {
        // 处理订单创建事件
        handleOrderCreated(msg)
    })
}

func handleOrderCreated(msg event.OrderCreatedPayload) {
    // 事件处理逻辑
}
```

## 并发控制

### 1. UUID锁使用

✅ **正确的UUID锁使用方式**
```go
type Service struct {
    systemLock lock.Lock
}

func NewService() *Service {
    return &Service{
        systemLock: lock.NewSystemLock(),
    }
}

func (s *Service) OpenDesk(deskUuid uint64) error {
    // 获取锁
    s.systemLock.LockUuid(deskUuid)
    defer s.systemLock.UnlockUuid(deskUuid)
    
    // 业务逻辑
    return nil
}

// 资源销毁时清理锁
func (s *Service) DeleteDesk(deskUuid uint64) error {
    // 业务逻辑
    
    // 清理UUID锁资源
    s.systemLock.ClearUuidLock(deskUuid)
    return nil
}
```

### 2. Context传递规范

✅ **在协程中使用Context副本**
```go
func (s *Service) ProcessAsync(ctx context.Context) {
    go func() {
        // 获取Context副本用于协程
        ctxCopy := ctx.Copy()
        // 注意：不能对副本进行写操作
        s.processInBackground(ctxCopy)
    }()
}
```

## 性能优化

### 1. API响应时间要求

✅ **本地响应时间要求200ms以内**
```go
func (c *Controller) GetOrderList(ctx *gin.Context) {
    start := time.Now()
    defer func() {
        duration := time.Since(start)
        if duration > 200*time.Millisecond {
            logger.Logger.Warn("API响应时间过长", 
                zap.String("api", ctx.Request.URL.Path),
                zap.Duration("duration", duration),
            )
        }
    }()
    
    // 业务逻辑
}
```

### 2. 数据库查询优化

✅ **使用预加载避免N+1查询**
```go
func (r *orderRepo) GetOrderWithProducts(orderUuid uint64) (*model.Order, error) {
    var order model.Order
    err := r.db.Preload("Products").
        Preload("Products.Product").
        Where("uuid = ?", orderUuid).
        First(&order).Error
    return &order, errors.WithMessage(err)
}
```

✅ **使用索引和分页**
```go
func (r *orderRepo) GetOrderList(req req.OrderListReq) ([]model.Order, int64, error) {
    var orders []model.Order
    var total int64
    
    db := r.db.Model(&model.Order{}).
        Where("company_uuid = ?", req.CompanyUuid).
        Where("create_time >= ?", req.StartTime).
        Where("create_time <= ?", req.EndTime)
    
    // 先计算总数
    db.Count(&total)
    
    // 分页查询
    err := db.Offset((req.PageNo - 1) * req.PageSize).
        Limit(req.PageSize).
        Order("create_time DESC").
        Find(&orders).Error
    
    return orders, total, errors.WithMessage(err)
}
```

## 数据库操作规范

### 1. 数据库管理器使用

✅ **使用DBManager获取数据库连接**
```go
type orderSrv struct {
    dbm *database.DBManager
}

func (s *orderSrv) CreateOrder(ctx context.Context) error {
    // 获取对应公司的数据库连接
    db := s.dbm.GetDB(ctx.GetDbId())
    
    // 数据库操作
    err := db.Create(&order).Error
    return errors.WithMessage(err)
}
```

## Swagger文档规范

### 1. Swagger注解

✅ **使用swaggo/swag生成API文档**
```go
// CreateOrder 创建订单
// @Summary 创建订单
// @Description 创建一个新的订单
// @Tags 订单管理
// @Accept json
// @Produce json
// @Param request body req.CreateOrderReq true "创建订单请求"
// @Success 200 {object} resp.CreateOrderResp "成功"
// @Failure 400 {object} dto.Response "请求参数错误"
// @Security JwtToken
// @Router /api/v1/order/create [post]
func (c *Controller) CreateOrder(ctx *gin.Context) {
    // 实现
}
```

### 2. 数据验证

✅ **使用binding标签进行参数验证**
```go
type CreateOrderReq struct {
    ProductId uint64  `json:"product_id" binding:"required"`              // 商品ID，必填
    Quantity  int     `json:"quantity" binding:"required,min=1,max=999"`  // 数量，1-999
    Price     float64 `json:"price" binding:"required,gt=0"`              // 价格，大于0
    Remark    string  `json:"remark" binding:"omitempty,max=200"`         // 备注，最大200字符
}
```

## 安全规范

### 1. 操作权限验证

✅ **验证操作权限**
```go
func (s *orderSrv) CancelOrder(ctx context.Context, req req.OrderCancelReq) error {
    // 验证订单是否可以取消
    saleBill, err := s.IsCellCancelOrder(ctx, req.SaleBillUuid)
    if err != nil {
        return errors.WithMessage(err)
    }
    
    // 验证用户权限
    if !ctx.HasPermission("order.cancel") {
        return errors.New("无权限取消订单")
    }
    
    // 业务逻辑
    return nil
}
```

### 2. 参数验证

✅ **严格的参数验证**
```go
func (req *OrderCancelReq) Validate() error {
    if req.SaleBillUuid == 0 {
        return errors.New("销售账单UUID不能为空")
    }
    if req.CancelReason == "" {
        return errors.New("取消原因不能为空")
    }
    if len(req.CancelReason) > 500 {
        return errors.New("取消原因不能超过500字符")
    }
    return nil
}
```

## 多语言使用规范

### 1. 支持的语言列表

系统支持以下语言：
- `zh` - 简体中文
- `zhtw` - 繁体中文
- `en` - 英语
- `th` - 泰语
- `ja` - 日语
- `ko` - 韩语
- `my` - 缅甸语
- `tr` - 土耳其语
- `de` - 德语
- `sv` - 瑞典语

### 2. 获取当前语言

✅ **从Context中获取语言**
```go
func (s *orderSrv) CreateOrder(ctx context.Context) error {
    // 获取当前请求的语言
    language := ctx.GetLanguage()
    
    // 使用语言进行翻译
    message := i18n.Translate(language, "订单创建成功")
    
    return nil
}
```

✅ **从Gin Context中获取语言**
```go
import "ttpos-server-go/i18n"

func (c *Controller) GetOrder(ginCtx *gin.Context) {
    // 从请求头获取语言
    language := i18n.GetAcceptLanguage(ginCtx)
    
    // 使用语言
    message := i18n.Translate(language, "查询成功")
}
```

### 3. 使用i18n翻译

✅ **基础翻译**
```go
import "ttpos-server-go/i18n"

// 简单翻译
message := i18n.Translate(ctx.GetLanguage(), "操作成功")

// 带参数的翻译
message := i18n.Translate(
    ctx.GetLanguage(), 
    "物品 %s 库存不足", 
    materialName,
)

// 多个参数
message := i18n.Translate(
    ctx.GetLanguage(),
    "%s管理员添加会员发卡赠送操作 [%s]",
    adminName,
    operationType,
)
```

### 4. 多语言数据结构

✅ **使用LocaleResponse存储多语言内容**
```go
import "ttpos-server-go/app/dto"

type ProductResp struct {
    ProductId uint64              `json:"product_id"`
    Name      dto.LocaleResponse  `json:"name"`  // 多语言名称
}

// 构建多语言响应
product := ProductResp{
    ProductId: 1001,
    Name: dto.LocaleResponse{
        ZH:   "商品名称",
        EN:   "Product Name",
        TH:   "ชื่อสินค้า",
        JA:   "商品名",
        KO:   "상품명",
        MY:   "ကုန်ပစ္စည်းအမည်",
        TR:   "Ürün Adı",
        SV:   "Produktnamn",
        ZHTW: "商品名稱",
    },
}
```

### 5. LocaleResponse常用方法

✅ **LocaleResponse工具方法**
```go
// 获取指定语言的值
name := localeResp.GetLocale("zh")  // 返回中文

// 设置指定语言的值
localeResp.SetLocale("en", "Product Name")

// 转换为JSON字符串
jsonStr := localeResp.ToJson()

// 检查是否为空
if localeResp.IsNull() {
    // 所有语言都为空
}

// 检查是否包含必需的语言
requiredLocales := []string{"zh", "en", "th"}
if !localeResp.CheckRequiredLocale(requiredLocales) {
    return errors.New("缺少必需的语言翻译")
}
```

### 6. 自动翻译服务

✅ **使用翻译服务自动翻译**
```go
// 添加需要翻译的多语言UUID到队列
err := translateSrv.AddMultiLanguageNameUuidToSet(
    companyUuid,
    multiLanguageNameUuid1,
    multiLanguageNameUuid2,
)

// 系统会自动异步翻译这些内容
```

### 7. 多语言JSON解析

✅ **从JSON字符串解析多语言**
```go
import "ttpos-server-go/pkg/language"

// JSON字符串
jsonStr := `{"zh":"商品","en":"Product","th":"สินค้า"}`

// 解析为LocaleResponse
locale := language.JsonToLocaleResponse(jsonStr)

// 使用
zhName := locale.ZH  // "商品"
enName := locale.EN  // "Product"
```

### 8. 语言包文件

语言包文件位于 `i18n/languages/` 目录：
```
i18n/languages/
├── zh.json      # 简体中文
├── zhtw.json    # 繁体中文
├── en.json      # 英语
├── th.json      # 泰语
├── ja.json      # 日语
├── ko.json      # 韩语
├── my.json      # 缅甸语
├── tr.json      # 土耳其语
├── de.json      # 德语
└── sv.json      # 瑞典语
```

✅ **语言包格式**
```json
{
  "操作成功": "操作成功",
  "操作失败": "操作失败",
  "物品 %s 库存不足": "物品 %s 库存不足",
  "订单创建成功": "订单创建成功"
}
```

### 9. 错误消息国际化

✅ **返回国际化错误消息**
```go
func (s *orderSrv) CancelOrder(ctx context.Context, req req.OrderCancelReq) error {
    // 验证订单状态
    if order.Status != constant.OrderStatusPending {
        return errors.New(
            i18n.Translate(ctx.GetLanguage(), "订单状态不允许取消"),
        )
    }
    
    // 带参数的错误消息
    if stock < required {
        return errors.New(
            i18n.Translate(
                ctx.GetLanguage(),
                "物品 %s 库存不足，当前库存：%s，需要：%s",
                materialName,
                strconv.Itoa(stock),
                strconv.Itoa(required),
            ),
        )
    }
    
    return nil
}
```

### 10. API响应国际化

✅ **返回多语言响应数据**
```go
// 方式1：返回所有语言
type ProductResp struct {
    ProductId uint64             `json:"product_id"`
    Name      dto.LocaleResponse `json:"name"`
}

// 方式2：只返回当前语言
type ProductResp struct {
    ProductId uint64 `json:"product_id"`
    Name      string `json:"name"`
}

func (s *productSrv) GetProduct(ctx context.Context) (*ProductResp, error) {
    product := getProductFromDB()
    
    // 解析多语言
    locale := language.JsonToLocaleResponse(product.Name)
    
    return &ProductResp{
        ProductId: product.ProductId,
        Name:      locale.GetLocale(ctx.GetLanguage()),  // 只返回当前语言
    }, nil
}
```

### 11. 最佳实践

✅ **多语言使用建议**
1. **所有面向用户的文本都要支持多语言**
   - 错误消息
   - 提示信息
   - 状态文本
   - 按钮文本

2. **使用翻译键而不是硬编码文本**
   ```go
   // ✅ 正确
   message := i18n.Translate(ctx.GetLanguage(), "操作成功")
   
   // ❌ 错误
   message := "操作成功"
   ```

3. **新增翻译键时同步更新所有语言包**
   - 在 `i18n/languages/` 下的所有JSON文件中添加对应的翻译

4. **参数化的翻译使用占位符**
   ```go
   // ✅ 正确 - 使用 %s 占位符
   i18n.Translate(ctx.GetLanguage(), "物品 %s 库存不足", name)
   
   // ❌ 错误 - 字符串拼接
   i18n.Translate(ctx.GetLanguage(), "物品") + name + i18n.Translate(ctx.GetLanguage(), "库存不足")
   ```

5. **数据库存储使用LocaleResponse的JSON格式**
   ```go
   // 存储
   nameJson := localeResp.ToJson()
   db.Model(&Product{}).Update("name", nameJson)
   
   // 读取
   locale := language.JsonToLocaleResponse(product.Name)
   ```

## 代码提交规范

### 1. 提交前检查
- 执行 `go mod tidy` 整理依赖
- 执行 `go fmt` 格式化代码
- 执行 `go vet` 检查代码问题
- 确保所有测试通过

### 2. 忽略文件
禁止提交以下文件和目录：
- `.idea/`、`.vscode/` - IDE配置文件
- `tmp/`、`bin/`、`__debug_*` - 构建产物和临时文件
- `*.log` - 日志文件
- 包含敏感信息的配置文件

---

**最后更新**: 2025-11-16  
**维护者**: TTPOS Team

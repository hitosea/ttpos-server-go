# API 设计规范

> 本规范基于 main 项目（Go/Gin）的现有实现，适用于所有模块的API接口设计
> 
> **注意**: PHP项目将被逐步淘汰，所有新功能和规范以 Go 项目为准

## 📋 目录

1. [RESTful API设计](#restful-api设计)
2. [URL命名规范](#url命名规范)
3. [HTTP方法](#http方法)
4. [请求格式](#请求格式)
5. [响应格式](#响应格式)
6. [错误处理](#错误处理)
7. [分页规范](#分页规范)
8. [认证授权](#认证授权)
9. [Swagger文档](#swagger文档)

## RESTful API设计

### 1. 资源命名

✅ **正确**: 使用名词复数形式
```
GET    /api/v1/users           # 获取用户列表
GET    /api/v1/users/123       # 获取单个用户
POST   /api/v1/users           # 创建用户
PUT    /api/v1/users/123       # 更新用户
DELETE /api/v1/users/123       # 删除用户

GET    /api/v1/orders          # 获取订单列表
GET    /api/v1/orders/456      # 获取单个订单
```

❌ **错误**: 使用动词或单数形式
```
GET    /api/v1/getUsers        # ❌ 使用动词
GET    /api/v1/user            # ❌ 使用单数
POST   /api/v1/createUser      # ❌ 使用动词
```

### 2. 资源层级

✅ **正确**: 体现资源关系
```
GET    /api/v1/users/123/orders           # 获取用户的订单
GET    /api/v1/orders/456/products        # 获取订单的商品
GET    /api/v1/categories/789/products    # 获取分类下的商品
```

❌ **错误**: 层级过深
```
GET    /api/v1/companies/1/shops/2/users/3/orders/4/products/5  # ❌ 太深
```

**建议**: 最多3层，超过3层使用查询参数
```
GET    /api/v1/products?category_id=789&shop_id=2
```

## URL命名规范

### 1. 使用 snake_case

✅ **正确**: 使用下划线分隔
```
/api/v1/user_profiles
/api/v1/order_items
/api/v1/product_categories
/api/v1/payment_methods
```

❌ **错误**: 使用其他命名方式
```
/api/v1/userProfiles           # ❌ 驼峰命名
/api/v1/user-profiles          # ❌ 烤串命名
/api/v1/UserProfiles           # ❌ 大驼峰
```

### 2. 版本控制

✅ **正确**: 在URL中包含版本号
```
/api/v1/users
/api/v2/users
```

### 3. 查询参数

✅ **正确**: 使用 snake_case
```
GET /api/v1/users?page=1&page_size=20&sort_by=create_time&order=desc
GET /api/v1/products?category_id=123&min_price=100&max_price=500
```

## HTTP方法

### 1. 标准方法

| 方法 | 用途 | 示例 |
|------|------|------|
| GET | 获取资源 | `GET /api/v1/users` |
| POST | 创建资源 | `POST /api/v1/users` |
| PUT | 完整更新资源 | `PUT /api/v1/users/123` |
| PATCH | 部分更新资源 | `PATCH /api/v1/users/123` |
| DELETE | 删除资源 | `DELETE /api/v1/users/123` |

### 2. 幂等性

✅ **遵循幂等性原则**:
- GET、PUT、DELETE 应该是幂等的
- POST 不是幂等的
- 多次执行相同的PUT/DELETE请求，结果应该相同

### 3. 特殊操作

✅ **使用动词表示特殊操作**
```
POST   /api/v1/users/123/activate      # 激活用户
POST   /api/v1/orders/456/cancel       # 取消订单
POST   /api/v1/orders/456/refund       # 退款
POST   /api/v1/users/123/reset_password # 重置密码
```

## 请求格式

### 1. Content-Type

✅ **使用 JSON 格式**
```http
Content-Type: application/json
```

### 2. 请求体示例

✅ **正确的请求格式**
```json
{
  "username": "test_user",
  "email": "test@example.com",
  "password": "123456",
  "phone": "13800138000"
}
```

### 3. 请求头

✅ **必需的请求头**
```http
Content-Type: application/json
Authorization: Bearer <token>
Accept-Language: zh-CN
```

## 响应格式

### 1. 统一响应结构

✅ **Go项目标准响应格式**
```go
// 响应结构定义 (main/app/dto/common_resp.go)
type Response struct {
    Code    int    `json:"code"`    // 状态码: 1-成功, 0-失败
    Message string `json:"message"` // 提示信息
    Data    any    `json:"data"`    // 响应数据，必须是对象，不能是null或数组
}
```

✅ **正确的响应格式**
```json
{
  "code": 1,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1,
        "username": "test_user",
        "email": "test@example.com"
      }
    ],
    "meta": {
      "page_no": 1,
      "page_size": 20,
      "total": 100
    }
  }
}
```

❌ **错误的响应格式**
```json
{
  "code": 1,
  "message": "success",
  "data": null              // ❌ data不能为null
}

{
  "code": 1,
  "message": "success",
  "data": []                // ❌ data不能是数组
}
```

### 2. 使用 helper 返回响应

✅ **在 Handler 中使用 helper**
```go
import (
    "ttpos-server-go/app/api/helper"
    "ttpos-server-go/app/constant"
    "github.com/gin-gonic/gin"
)

// 成功响应
helper.Success(c, data)

// 失败响应
helper.ErrorWithDetail(c, constant.CodeFail, errors.WithMessage(err))

// 空数据成功响应
helper.Success(c, gin.H{})
```

### 3. 成功响应示例

✅ **列表数据响应**
```go
// 定义响应结构 (main/app/dto/resp/)
type OrderListPaginationResp struct {
    List []BillLists   `json:"list"` // 订单列表
    Meta OrderListMeta `json:"meta"` // Meta信息
}

type OrderListMeta struct {
    dto.PageResponse
    TotalNum    int64 `json:"total_num"`    // 总数量
    UnpaidNum   int64 `json:"unpaid_num"`   // 待付款数量
    CompleteNum int64 `json:"complete_num"` // 已完成数量
    CancelNum   int64 `json:"cancel_num"`   // 已取消数量
}
```

JSON响应:
```json
{
  "code": 1,
  "message": "success",
  "data": {
    "list": [
      {"id": 1, "name": "商品1"},
      {"id": 2, "name": "商品2"}
    ],
    "meta": {
      "page_no": 1,
      "page_size": 20,
      "total": 100,
      "total_num": 100,
      "unpaid_num": 10,
      "complete_num": 85,
      "cancel_num": 5
    }
  }
}
```

✅ **单条数据响应**
```go
// 定义响应结构
type CreateOrderResp struct {
    Uuid uint64 `json:"uuid"` // 订单UUID
}

// 返回
helper.Success(c, resp.CreateOrderResp{Uuid: orderUuid})
```

JSON响应:
```json
{
  "code": 1,
  "message": "success",
  "data": {
    "uuid": 123456789
  }
}
```

✅ **无数据返回**
```go
// 删除、更新等操作成功
helper.Success(c, gin.H{})
```

JSON响应:
```json
{
  "code": 1,
  "message": "操作成功",
  "data": {}
}
```

### 4. HTTP状态码

**注意**: 在 main 项目中，HTTP状态码统一使用 200，通过响应体中的 `code` 字段区分成功和失败

| HTTP状态码 | code | 说明 | 使用场景 |
|-----------|------|------|----------|
| 200 | 1 | 成功 | 所有成功的请求 |
| 200 | 0 | 失败 | 业务逻辑失败 |
| 200 | 400 | 参数错误 | 请求参数验证失败 |
| 200 | 401 | 未认证 | Token无效或过期 |
| 200 | 403 | 无权限 | 没有操作权限 |
| 200 | 404 | 不存在 | 资源不存在 |

✅ **正确使用方式**
```go
// 成功: HTTP 200, code=1
helper.Success(c, data)

// 失败: HTTP 200, code=0
helper.ErrorWithDetail(c, constant.CodeFail, errors.WithMessage(err))

// 参数错误: HTTP 200, code=400
helper.HandleValidationError(c, err, req, validationMessages)
```

## 错误处理

### 1. 错误响应格式

✅ **正确的错误响应**
```json
{
  "code": 0,
  "message": "用户名已存在",
  "data": {}
}
```

### 2. 验证错误

✅ **字段验证错误**
```json
{
  "code": 0,
  "message": "参数验证失败",
  "data": {
    "errors": {
      "username": ["用户名不能为空", "用户名长度为2-20个字符"],
      "email": ["邮箱格式不正确"]
    }
  }
}
```

### 3. 错误码定义

✅ **统一的错误码**
```javascript
const ERROR_CODES = {
  // 通用错误 (0-999)
  SUCCESS: 1,
  ERROR: 0,
  PARAM_ERROR: 400,
  UNAUTHORIZED: 401,
  FORBIDDEN: 403,
  NOT_FOUND: 404,
  
  // 用户相关 (1000-1999)
  USER_NOT_FOUND: 1001,
  USER_ALREADY_EXISTS: 1002,
  PASSWORD_ERROR: 1003,
  
  // 订单相关 (2000-2999)
  ORDER_NOT_FOUND: 2001,
  ORDER_CANNOT_CANCEL: 2002,
  ORDER_ALREADY_PAID: 2003,
};
```

## 分页规范

### 1. 分页参数

✅ **使用统一的分页参数**
```
GET /api/v1/cashier/order/list?page=1&page_size=20
```

| 参数 | 类型 | 说明 | 默认值 |
|------|------|------|--------|
| page | int | 页码（从1开始） | 1 |
| page_size | int | 每页数量 | 20 |

✅ **分页请求定义**
```go
// main/app/dto/common_req.go
type PageReq struct {
    Page     int `json:"page" form:"page" binding:"omitempty,min=1"`           // 页码
    PageSize int `json:"page_size" form:"page_size" binding:"omitempty,min=1"` // 每页大小
}
```

### 2. 分页响应

✅ **分页响应结构定义**
```go
// main/app/dto/common_resp.go
type PageResponse struct {
    PageNo   int   `json:"page_no"`   // 当前页码
    PageSize int   `json:"page_size"` // 每页大小
    Total    int64 `json:"total"`     // 总数
}
```

✅ **分页信息放在 meta 中**
```json
{
  "code": 1,
  "message": "success",
  "data": {
    "list": [...],
    "meta": {
      "page_no": 1,
      "page_size": 20,
      "total": 100
    }
  }
}
```

### 3. 排序参数

✅ **使用 sort_by 和 order**
```
GET /api/v1/users?sort_by=create_time&order=desc
GET /api/v1/products?sort_by=price&order=asc
```

## Handler 和路由规范

### 1. Handler 结构定义

✅ **Handler 结构规范**
```go
// main/app/api/v1/cashier/cashier_order.go

// OrderHandler 收银点餐处理程序
type OrderHandler struct {
    orderSrv service.IOrderSrv // 订单服务
    deskSrv  service.IDeskSrv  // 桌台服务
}

// NewOrderHandler 创建订单处理程序
func NewOrderHandler(dbm *database.DBManager, cache cache.Cache) *OrderHandler {
    return &OrderHandler{
        orderSrv: service.NewOrderSrv(dbm, cache),
        deskSrv:  service.NewDeskSrv(dbm, cache),
    }
}
```

### 2. 路由注册规范

✅ **路由注册函数**
```go
// main/app/api/v1/cashier/handler.go

// RegisterOrderHandlers 注册订单相关路由
func RegisterOrderHandlers(router *gin.RouterGroup, dbm *database.DBManager, cache cache.Cache) {
    handler := NewOrderHandler(dbm, cache)
    
    // 使用中间件
    auth := router.Group("", middleware.JWTAuth())
    {
        auth.GET("/order/list", handler.GetCashierOrderList)
        auth.GET("/order/info", handler.GetOrderInfo)
        auth.POST("/order/cancel", handler.CancelOrder)
        auth.GET("/order/return", handler.ReturnOrderInfo)
        auth.POST("/order/return", handler.ReturnOrder)
    }
}
```

✅ **在 router.go 中注册**
```go
// main/router/router.go

func Setup(r *gin.Engine, dbm *database.DBManager, cache cache.Cache) {
    apiV1 := r.Group("api/v1")
    {
        // 收银端
        cashierGroup := apiV1.Group("/cashier")
        {
            cashier.RegisterOrderHandlers(cashierGroup, dbm, cache)
            cashier.RegisterProductHandlers(cashierGroup, dbm, cache)
            // ... 其他模块
        }
        
        // 商家端
        shopGroup := apiV1.Group("/shop")
        {
            shop.RegisterOrderHandlers(shopGroup, dbm, cache)
            // ... 其他模块
        }
    }
}
```

### 3. Handler 方法规范

✅ **标准 Handler 方法结构**
```go
func (h *OrderHandler) GetOrderInfo(c *gin.Context) {
    // 1. 获取上下文
    ctx := helper.GetContext(c)
    
    // 2. 绑定请求参数
    req := req.OrderInfoReq{}
    if err := c.ShouldBindQuery(&req); err != nil {
        helper.HandleValidationError(c, err, req, nil)
        return
    }
    
    // 3. 调用服务层
    res, err := h.orderSrv.GetOrderInfos(ctx, req)
    if err != nil {
        helper.ErrorWithDetail(c, constant.CodeFail, errors.WithMessage(err))
        return
    }
    
    // 4. 返回结果
    helper.Success(c, res)
}
```

### 4. 参数绑定规范

✅ **Query 参数绑定**
```go
// GET /api/v1/cashier/order/list?page=1&page_size=20
req := req.OrderListReq{}
if err := c.ShouldBindQuery(&req); err != nil {
    helper.HandleValidationError(c, err, req, dto.PageReqMessage)
    return
}
```

✅ **Body 参数绑定**
```go
// POST /api/v1/cashier/order/cancel
req := req.OrderCancelReq{}
if err := c.ShouldBindJSON(&req); err != nil {
    helper.HandleValidationError(c, err, req, nil)
    return
}
```

✅ **Path 参数绑定**
```go
// GET /api/v1/cashier/order/:id
id := c.Param("id")
orderUuid, err := strconv.ParseUint(id, 10, 64)
if err != nil {
    helper.ErrorWithDetail(c, constant.CodeFail, errors.New("无效的订单ID"))
    return
}
```

## 认证授权

### 1. JWT认证

✅ **使用 Bearer Token**
```http
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

### 2. 认证流程

```
1. 用户登录
POST /api/v1/auth/login
{
  "username": "test",
  "password": "123456"
}

响应:
{
  "code": 1,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "expires_in": 7200
  }
}

2. 使用Token访问API
GET /api/v1/users
Headers:
  Authorization: Bearer <token>

3. Token刷新
POST /api/v1/auth/refresh
Headers:
  Authorization: Bearer <old_token>

响应:
{
  "code": 1,
  "message": "刷新成功",
  "data": {
    "token": "new_token...",
    "expires_in": 7200
  }
}
```

### 3. 权限验证

✅ **基于角色的访问控制（RBAC）**
```
用户 → 角色 → 权限

示例:
- 管理员角色: user.create, user.update, user.delete
- 普通用户角色: user.view
```

## 过滤和搜索

### 1. 过滤参数

✅ **使用查询参数过滤**
```
GET /api/v1/products?category_id=123&status=1&min_price=100&max_price=500
GET /api/v1/users?role=admin&status=active
```

### 2. 搜索参数

✅ **使用 keyword 或 q**
```
GET /api/v1/products?keyword=手机
GET /api/v1/users?q=张三
```

### 3. 日期范围

✅ **使用 start_time 和 end_time**
```
GET /api/v1/orders?start_time=1704038400&end_time=1704124800
```

## 批量操作

### 1. 批量创建

✅ **POST 请求，传递数组**
```json
POST /api/v1/users/batch

{
  "users": [
    {"username": "user1", "email": "user1@example.com"},
    {"username": "user2", "email": "user2@example.com"}
  ]
}
```

### 2. 批量更新

✅ **PATCH 请求，传递ID数组**
```json
PATCH /api/v1/users/batch

{
  "ids": [1, 2, 3],
  "data": {
    "status": 1
  }
}
```

### 3. 批量删除

✅ **DELETE 请求，传递ID数组**
```json
DELETE /api/v1/users/batch

{
  "ids": [1, 2, 3]
}
```

## 文件上传

### 1. 单文件上传

✅ **使用 multipart/form-data**
```http
POST /api/v1/upload
Content-Type: multipart/form-data

file: <binary>
```

响应:
```json
{
  "code": 1,
  "message": "上传成功",
  "data": {
    "url": "https://example.com/uploads/file.jpg",
    "filename": "file.jpg",
    "size": 102400
  }
}
```

### 2. 多文件上传

✅ **使用数组形式**
```http
POST /api/v1/upload/batch
Content-Type: multipart/form-data

files[]: <binary>
files[]: <binary>
```

## 国际化

### 1. 语言参数

✅ **使用 Accept-Language 请求头**
```http
Accept-Language: zh
Accept-Language: en
Accept-Language: th
```

### 2. LocaleResponse 多语言响应

✅ **多语言字段定义**
```go
// main/app/dto/common_resp.go
type LocaleResponse struct {
    ZH   string `json:"zh"`   // 中文
    TH   string `json:"th"`   // 泰语
    EN   string `json:"en"`   // 英语
    ZHTW string `json:"zhtw"` // 粤语
    JA   string `json:"ja"`   // 日语
    KO   string `json:"ko"`   // 韩语
    MY   string `json:"my"`   // 缅甸语
    TR   string `json:"tr"`   // 土耳其语
    SV   string `json:"sv"`   // 瑞典语
}
```

✅ **在响应中使用多语言字段**
```go
// 商品响应示例
type ProductResp struct {
    Uuid       uint64             `json:"uuid"`        // 商品UUID
    LocaleName dto.LocaleResponse `json:"locale_name"` // 多语言商品名称
    Price      float64            `json:"price"`       // 价格
}
```

JSON响应:
```json
{
  "code": 1,
  "message": "success",
  "data": {
    "uuid": 123456,
    "locale_name": {
      "zh": "炒饭",
      "th": "ข้าวผัด",
      "en": "Fried Rice",
      "zhtw": "炒飯",
      "ja": "チャーハン",
      "ko": "볶음밥",
      "my": "",
      "tr": "",
      "sv": ""
    },
    "price": 25.00
  }
}
```

### 3. 响应消息国际化

✅ **message 字段根据语言返回**
```json
// zh
{
  "code": 0,
  "message": "用户名不能为空",
  "data": {}
}

// en
{
  "code": 0,
  "message": "Username is required",
  "data": {}
}

// th
{
  "code": 0,
  "message": "ต้องระบุชื่อผู้ใช้",
  "data": {}
}
```

### 4. 支持的语言列表

| 语言代码 | 语言名称 | 字段名 |
|---------|---------|--------|
| zh | 简体中文 | ZH |
| th | 泰语 | TH |
| en | 英语 | EN |
| zhtw | 繁体中文（粤语） | ZHTW |
| ja | 日语 | JA |
| ko | 韩语 | KO |
| my | 缅甸语 | MY |
| tr | 土耳其语 | TR |
| sv | 瑞典语 | SV |

## Swagger文档

### 1. Swagger注解规范

✅ **main 项目 Swagger 注解示例**
```go
// GetCashierOrderList 处理获取订单列表
// @Summary 获取订单列表
// @Description 获取订单列表
// @Tags 收银端.订单
// @Accept json
// @Produce json
// @Security JwtToken
// @param data query req.OrderListReq true "列表参数"
// @Success 200 {object} resp.OrderListPaginationResp "订单列表"
// @Failure 404 {object} nil "未找到"
// @Router /cashier/order/list [get]
func (h *OrderHandler) GetCashierOrderList(c *gin.Context) {
    // 实现...
}
```

### 2. Swagger注解规则

✅ **必需的注解**
```go
// @Summary      简短描述（必需）
// @Description  详细描述（必需）
// @Tags         标签，格式: "模块.子模块"（必需）
// @Accept       接受的内容类型，通常是 json（必需）
// @Produce      返回的内容类型，通常是 json（必需）
// @Security     安全认证，通常是 JwtToken（需要认证时必需）
// @Router       路由路径和方法（必需）
```

✅ **参数注解**
```go
// Query参数
// @param data query req.OrderListReq true "列表参数"

// Body参数
// @param data body req.CreateOrderReq true "创建订单参数"

// Path参数
// @param id path uint64 true "订单ID"
```

✅ **响应注解**
```go
// 成功响应
// @Success 200 {object} resp.OrderListPaginationResp "订单列表"

// 失败响应
// @Failure 404 {object} nil "未找到"
// @Failure 400 {object} dto.Response "请求参数错误"
```

### 3. Tags命名规范

✅ **使用中文，格式: "端.模块"**
```go
// @Tags 收银端.订单
// @Tags 商家端.订单
// @Tags 点餐助手端.桌台
// @Tags 会员端.商品
// @Tags 厨房端.基础
```

### 4. 完整示例

✅ **GET请求示例**
```go
// GetOrderInfo 处理获取订单详情
// @Summary 获取订单详情
// @Description 获取订单详情
// @Tags 收银端.订单
// @Accept json
// @Produce json
// @Security JwtToken
// @param data query req.OrderInfoReq true "详情参数"
// @Success 200 {object} resp.OrderInfosResp "订单详情"
// @Failure 404 {object} nil "未找到"
// @Router /cashier/order/info [get]
func (h *OrderHandler) GetOrderInfo(c *gin.Context) {
    ctx := helper.GetContext(c)
    req := req.OrderInfoReq{}
    if err := c.ShouldBindQuery(&req); err != nil {
        helper.HandleValidationError(c, err, req, nil)
        return
    }
    res, err := h.orderSrv.GetOrderInfos(ctx, req)
    if err != nil {
        helper.ErrorWithDetail(c, constant.CodeFail, errors.WithMessage(err))
        return
    }
    helper.Success(c, res)
}
```

✅ **POST请求示例**
```go
// CancelOrder 处理取消订单
// @Summary 取消订单
// @Description 取消订单
// @Tags 收银端.订单
// @Accept json
// @Produce json
// @Security JwtToken
// @param data body req.OrderCancelReq true "详情参数"
// @Success 200 {object} nil "取消订单成功"
// @Failure 404 {object} nil "未找到"
// @Router /cashier/order/cancel [post]
func (h *OrderHandler) CancelOrder(c *gin.Context) {
    ctx := helper.GetContext(c)
    req := req.OrderCancelReq{}
    if err := c.ShouldBindJSON(&req); err != nil {
        helper.HandleValidationError(c, err, req, nil)
        return
    }
    err := h.orderSrv.CancelOrder(ctx, req)
    if err != nil {
        helper.ErrorWithDetail(c, constant.CodeFail, errors.WithMessage(err))
        return
    }
    helper.Success(c, gin.H{})
}
```

## 性能优化

### 1. 字段选择

✅ **使用 fields 参数**
```
GET /api/v1/users?fields=id,username,email
```

### 2. 关联数据

✅ **使用 include 参数**
```
GET /api/v1/users/123?include=roles,permissions
```

### 3. 缓存

✅ **使用 ETag 和 Last-Modified**
```http
Response Headers:
ETag: "33a64df551425fcc55e4d42a148795d9f25f89d4"
Last-Modified: Wed, 21 Oct 2025 07:28:00 GMT
Cache-Control: max-age=3600
```

## 限流

### 1. 速率限制

✅ **返回限流信息**
```http
Response Headers:
X-RateLimit-Limit: 1000
X-RateLimit-Remaining: 999
X-RateLimit-Reset: 1372700873
```

### 2. 超出限制

```json
HTTP/1.1 429 Too Many Requests

{
  "code": 429,
  "message": "请求过于频繁，请稍后再试",
  "data": {
    "retry_after": 60
  }
}
```

## 最佳实践

### 1. 版本管理

✅ **URL版本控制**
```
/api/v1/cashier/order/list
/api/v1/shop/order/list
/api/v2/cashier/order/list  // 新版本
```

- ✅ 使用URL版本控制：`/api/v1/`
- ✅ 保持向后兼容
- ✅ 废弃的API提前通知

### 2. 安全性

✅ **输入验证**
```go
// 使用 binding 标签验证
type CreateOrderReq struct {
    ProductId uint64  `json:"product_id" binding:"required"`
    Quantity  int     `json:"quantity" binding:"required,min=1,max=999"`
    Price     float64 `json:"price" binding:"required,gt=0"`
}
```

✅ **中间件保护**
```go
// JWT认证中间件
auth := router.Group("", middleware.JWTAuth())

// 内部接口保护
internalGroup := apiV1.Group("/internal")
internalGroup.Use(middleware.Internal())
```

- ✅ 使用HTTPS
- ✅ 验证所有输入
- ✅ 使用GORM防止SQL注入
- ✅ 实施JWT认证

### 3. 性能

✅ **数据库优化**
```go
// 使用预加载避免N+1查询
db.Preload("Products").
   Preload("Products.Product").
   Find(&orders)

// 使用索引和分页
db.Where("company_uuid = ?", companyUuid).
   Offset((page - 1) * pageSize).
   Limit(pageSize).
   Find(&orders)
```

✅ **缓存使用**
```go
// 使用Redis缓存
cache.Set(ctx, key, value, expiration)
value, err := cache.Get(ctx, key)
```

- ✅ 使用分页（默认20条/页）
- ✅ 使用Redis缓存
- ✅ 数据库连接池管理
- ✅ API响应时间控制在200ms内

### 4. 可维护性

✅ **代码组织**
```
main/
├── app/
│   ├── api/v1/          # API层
│   │   ├── cashier/     # 收银端
│   │   ├── shop/        # 商家端
│   │   └── assistant/   # 助手端
│   ├── service/         # 服务层
│   ├── repository/      # 数据访问层
│   ├── dto/             # 数据传输对象
│   │   ├── req/         # 请求
│   │   └── resp/        # 响应
│   └── model/           # 数据模型
```

✅ **注释规范**
```go
// GetOrderInfo 处理获取订单详情
// @Summary 获取订单详情
// @Description 获取订单详情
// @Tags 收银端.订单
// ...
func (h *OrderHandler) GetOrderInfo(c *gin.Context) {
    // 1. 获取上下文
    ctx := helper.GetContext(c)
    
    // 2. 绑定请求参数
    req := req.OrderInfoReq{}
    
    // 3. 调用服务层
    res, err := h.orderSrv.GetOrderInfos(ctx, req)
    
    // 4. 返回结果
    helper.Success(c, res)
}
```

- ✅ 完整的Swagger文档
- ✅ 清晰的中文注释
- ✅ 详细的错误信息
- ✅ 统一的响应格式
- ✅ 分层架构设计

### 5. 错误处理

✅ **统一错误处理**
```go
// 使用 errors.WithMessage 包装错误
if err != nil {
    return errors.WithMessage(err, "查询订单失败")
}

// 在Handler中统一处理
if err != nil {
    helper.ErrorWithDetail(c, constant.CodeFail, errors.WithMessage(err))
    return
}
```

✅ **参数验证错误**
```go
// 使用 HandleValidationError 处理验证错误
if err := c.ShouldBindJSON(&req); err != nil {
    helper.HandleValidationError(c, err, req, validationMessages)
    return
}
```

### 6. 日志记录

✅ **关键操作日志**
```go
// 记录订单操作
logger.Info("取消订单",
    zap.Uint64("order_uuid", orderUuid),
    zap.Uint64("company_uuid", companyUuid),
    zap.String("reason", cancelReason),
)

// 记录错误
logger.Error("订单查询失败",
    zap.Error(err),
    zap.Uint64("order_uuid", orderUuid),
)
```

---

**最后更新**: 2025-11-16  
**维护者**: TTPOS Team  
**基于**: main 项目（Go/Gin）实际实现

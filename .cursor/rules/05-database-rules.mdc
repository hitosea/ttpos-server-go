# 数据库开发规范

> 本规范适用于所有模块的数据库设计和操作

## 📋 目录

1. [多租户架构](#多租户架构)
2. [命名规范](#命名规范)
3. [字段规范](#字段规范)
4. [索引规范](#索引规范)
5. [迁移文件规范](#迁移文件规范)
6. [数据类型选择](#数据类型选择)
7. [性能优化](#性能优化)

## 多租户架构

### 1. 独立数据库模式

✅ **每个商户一个独立数据库**
```
shop8267304538112000     # 商户1的数据库
shop8609817471094784     # 商户2的数据库
shop8609817471094785     # 商户3的数据库
...
```

**优点**：
- 数据完全隔离，安全性高
- 便于数据迁移（库对库copy）
- 便于备份和恢复
- 便于扩展（可分布到不同服务器）

### 2. 系统数据库

```
saas             # 系统主库
├── 系统配置
├── 字典数据
├── 商户信息
└── 全局数据
```

## 命名规范

### 1. 数据库命名

✅ **正确**：
```sql
shop8267304538112000    -- 商户数据库（shop + 商户ID）
shop8609817471094784    -- 商户数据库
saas                    -- 系统数据库
```

**命名规则**：
- 商户数据库：`shop{商户ID}`，不使用下划线或连字符
- 系统数据库：`saas`

❌ **错误**：
```sql
ttpos_shop_1        -- 不要使用 ttpos_ 前缀和下划线
shop-123456         -- 不要使用连字符
shopDB              -- 不要使用驼峰命名
```

### 2. 表命名

✅ **正确**：使用 `ttpos_` 前缀 + 小写下划线
```sql
ttpos_product              -- 商品表
ttpos_sale_order           -- 销售订单表
ttpos_product_category     -- 商品分类表
```

❌ **错误**：
```sql
Product                    -- 大写
product                    -- 缺少前缀
ttpos-product              -- 使用连字符
ttposProduct               -- 驼峰命名
```

### 3. 字段命名

✅ **正确**：小写下划线（snake_case）
```sql
user_id                    -- 用户ID
product_name               -- 商品名称
create_time                -- 创建时间
order_amount               -- 订单金额
```

❌ **错误**：
```sql
userId                     -- 驼峰命名
ProductName                -- 大写
order-amount               -- 连字符
```

### 4. 索引命名

✅ **正确**：
```sql
PRIMARY KEY: id
UNIQUE KEY: uk_字段名
INDEX: idx_字段名
FOREIGN KEY: fk_表名_字段名
```

**示例**：
```sql
PRIMARY KEY (`id`)
UNIQUE KEY `uk_uuid` (`uuid`)
UNIQUE KEY `uk_username` (`username`)
KEY `idx_company_id` (`company_id`)
KEY `idx_create_time` (`create_time`)
KEY `idx_status_type` (`status`, `type`)
```

## 字段规范

### 1. 必需字段

✅ **每个表必须包含以下字段**：
```sql
CREATE TABLE `ttpos_example` (
    `id` bigint unsigned NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `uuid` bigint unsigned NOT NULL DEFAULT 0 COMMENT '唯一标识',
    `create_time` int NOT NULL DEFAULT 0 COMMENT '创建时间',
    `update_time` int NOT NULL DEFAULT 0 COMMENT '更新时间',
    `delete_time` int NOT NULL DEFAULT 0 COMMENT '删除时间（软删除）',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_uuid` (`uuid`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='示例表';
```

### 2. 时间字段

✅ **正确**：使用 `int` 类型存储 Unix 时间戳
```sql
`create_time` int NOT NULL DEFAULT 0 COMMENT '创建时间'
`update_time` int NOT NULL DEFAULT 0 COMMENT '更新时间'
`delete_time` int NOT NULL DEFAULT 0 COMMENT '删除时间'
`start_time` int NOT NULL DEFAULT 0 COMMENT '开始时间'
`end_time` int NOT NULL DEFAULT 0 COMMENT '结束时间'
```

❌ **错误**：不要使用 `datetime` 或 `timestamp`
```sql
`create_time` datetime DEFAULT NULL       -- ❌
`update_time` timestamp DEFAULT NULL      -- ❌
```

**原因**：
- int 类型占用空间小（4字节）
- 不受时区影响
- 便于计算和比较
- 默认值为 0 表示未设置

### 3. 金额字段

✅ **正确**：使用 `decimal` 类型
```sql
-- 高精度金额（8位小数）
`amount` decimal(20,8) NOT NULL DEFAULT 0.00000000 COMMENT '金额'

-- 普通金额（2位小数）
`price` decimal(14,2) NOT NULL DEFAULT 0.00 COMMENT '价格'
`total` decimal(14,2) NOT NULL DEFAULT 0.00 COMMENT '总计'
```

❌ **错误**：不要使用 `float` 或 `double`
```sql
`amount` float DEFAULT 0                  -- ❌ 精度丢失
`price` double DEFAULT 0                  -- ❌ 精度丢失
```

**选择标准**：
- `decimal(20,8)`：需要高精度的场景（汇率、积分等）
- `decimal(14,2)`：普通金额场景（价格、总额等）

### 4. UUID字段

✅ **正确**：使用 `bigint` 类型
```sql
`uuid` bigint unsigned NOT NULL DEFAULT 0 COMMENT '唯一标识'
`company_uuid` bigint unsigned NOT NULL DEFAULT 0 COMMENT '公司UUID'
`user_uuid` bigint unsigned NOT NULL DEFAULT 0 COMMENT '用户UUID'
```

❌ **错误**：不要使用 `varchar` 或 `char`
```sql
`uuid` varchar(32) DEFAULT NULL           -- ❌ 占用空间大
`uuid` char(36) DEFAULT NULL              -- ❌ 占用空间大
```

### 5. 状态字段

✅ **正确**：使用 `tinyint` 类型
```sql
`status` tinyint NOT NULL DEFAULT 0 COMMENT '状态：0-禁用 1-启用'
`type` tinyint NOT NULL DEFAULT 0 COMMENT '类型：1-类型A 2-类型B'
`is_deleted` tinyint NOT NULL DEFAULT 0 COMMENT '是否删除：0-否 1-是'
```

### 6. 文本字段

✅ **正确**：根据长度选择类型
```sql
-- 短文本（<255字符）
`name` varchar(100) NOT NULL DEFAULT '' COMMENT '名称'
`code` varchar(50) NOT NULL DEFAULT '' COMMENT '编码'

-- 中等文本（<65535字符）
`description` text COMMENT '描述'
`remark` text COMMENT '备注'

-- 长文本（>65535字符）
`content` longtext COMMENT '内容'
```

❌ **错误**：text类型不要设置默认值
```sql
`description` text DEFAULT ''             -- ❌ text不能有默认值
```

## 索引规范

### 1. 主键索引

✅ **每个表必须有主键**
```sql
PRIMARY KEY (`id`)
```

### 2. 唯一索引

✅ **唯一字段添加唯一索引**
```sql
UNIQUE KEY `uk_uuid` (`uuid`)
UNIQUE KEY `uk_username` (`username`)
UNIQUE KEY `uk_email` (`email`)
```

### 3. 普通索引

✅ **常用查询字段添加索引**
```sql
-- 单列索引
KEY `idx_company_id` (`company_id`)
KEY `idx_user_id` (`user_id`)
KEY `idx_create_time` (`create_time`)
KEY `idx_status` (`status`)

-- 复合索引（注意顺序）
KEY `idx_company_status` (`company_id`, `status`)
KEY `idx_user_time` (`user_id`, `create_time`)
```

### 4. 索引设计原则

✅ **遵循以下原则**：
- 为 WHERE、ORDER BY、GROUP BY 中的字段添加索引
- 复合索引遵循最左前缀原则
- 选择性高的字段放在复合索引前面
- 避免过多索引（影响写入性能）
- 定期分析和优化索引

## 迁移文件规范

### 1. 迁移文件命名

✅ **正确**：使用时间戳前缀
```
20250721370408_create_product_table.php
20250721370409_add_status_to_user.php
20250721370410_create_order_table.php
```

**生成方式**：
```bash
# PHP项目（admin/）
cd admin
php think migrate:create CreateProductTable

# 文件名前缀使用：date +%Y%m%d%H%M%S
```

### 2. 迁移文件结构

✅ **正确的迁移文件**：
```php
<?php

use think\migration\Migrator;
use think\migration\db\Column;

class CreateProductTable extends Migrator
{
    /**
     * 执行迁移
     */
    public function change()
    {
        // 检查表是否存在
        if ($this->hasTable('ttpos_product')) {
            return;
        }
        
        $table = $this->table('ttpos_product', [
            'engine' => 'InnoDB',
            'collation' => 'utf8mb4_unicode_ci',
            'comment' => '商品表',
        ]);
        
        $table
            ->addColumn('uuid', 'biginteger', ['signed' => false, 'default' => 0, 'comment' => '唯一标识'])
            ->addColumn('product_name', 'string', ['limit' => 100, 'default' => '', 'comment' => '商品名称'])
            ->addColumn('price', 'decimal', ['precision' => 14, 'scale' => 2, 'default' => 0.00, 'comment' => '价格'])
            ->addColumn('status', 'integer', ['limit' => \Phinx\Db\Adapter\MysqlAdapter::INT_TINY, 'default' => 1, 'comment' => '状态：0-禁用 1-启用'])
            ->addColumn('create_time', 'integer', ['default' => 0, 'comment' => '创建时间'])
            ->addColumn('update_time', 'integer', ['default' => 0, 'comment' => '更新时间'])
            ->addColumn('delete_time', 'integer', ['default' => 0, 'comment' => '删除时间'])
            ->addIndex(['uuid'], ['unique' => true, 'name' => 'uk_uuid'])
            ->addIndex(['product_name'], ['name' => 'idx_product_name'])
            ->addIndex(['status'], ['name' => 'idx_status'])
            ->create();
    }
}
```

### 3. 添加字段迁移

✅ **正确**：检查字段是否存在
```php
public function change()
{
    $table = $this->table('ttpos_product');
    
    // 检查字段是否存在
    if (!$table->hasColumn('category_id')) {
        $table->addColumn('category_id', 'biginteger', [
            'signed' => false,
            'default' => 0,
            'comment' => '分类ID',
            'after' => 'product_name'
        ])->update();
    }
    
    // 检查索引是否存在
    if (!$table->hasIndex(['category_id'])) {
        $table->addIndex(['category_id'], ['name' => 'idx_category_id'])
              ->update();
    }
}
```

### 4. 迁移最佳实践

✅ **遵循以下原则**：
- 每个迁移文件只做一件事
- 必须检查表/字段/索引是否存在
- 使用 `addColumn` 时不要换行（项目规范）
- 添加详细的中文注释
- 同步更新 `seeds/shop_01.sql` 文件
- 同步更新 `main/app/model/` 目录下的模型文件

## 数据类型选择

### 1. 整数类型

| 类型 | 字节 | 范围 | 使用场景 |
|------|------|------|----------|
| tinyint | 1 | -128~127 | 状态、类型、布尔值 |
| smallint | 2 | -32768~32767 | 小范围数值 |
| int | 4 | -21亿~21亿 | 普通整数、时间戳 |
| bigint | 8 | 很大 | ID、UUID、大数值 |

✅ **使用建议**：
```sql
`id` bigint unsigned                      -- 主键
`uuid` bigint unsigned                    -- UUID
`status` tinyint                          -- 状态
`count` int                               -- 计数
`timestamp` int                           -- 时间戳
```

### 2. 字符串类型

| 类型 | 最大长度 | 使用场景 |
|------|----------|----------|
| char | 255 | 固定长度字符串 |
| varchar | 65535 | 可变长度字符串 |
| text | 65535 | 中等文本 |
| longtext | 4GB | 长文本 |

✅ **使用建议**：
```sql
`code` varchar(50)                        -- 编码
`name` varchar(100)                       -- 名称
`phone` varchar(20)                       -- 电话
`description` text                        -- 描述
`content` longtext                        -- 内容
```

### 3. 数值类型

| 类型 | 精度 | 使用场景 |
|------|------|----------|
| decimal(M,D) | 精确 | 金额、价格 |
| float | 单精度 | ❌ 不推荐 |
| double | 双精度 | ❌ 不推荐 |

✅ **使用建议**：
```sql
`price` decimal(14,2)                     -- 价格
`amount` decimal(20,8)                    -- 高精度金额
`rate` decimal(10,4)                      -- 比率
```

## 性能优化

### 1. 查询优化

✅ **使用索引**：
```sql
-- ✅ 使用索引字段
SELECT * FROM ttpos_product WHERE uuid = 123456;

-- ❌ 避免函数操作索引字段
SELECT * FROM ttpos_product WHERE DATE(create_time) = '2025-01-01';

-- ✅ 改为范围查询
SELECT * FROM ttpos_product WHERE create_time >= 1704038400 AND create_time < 1704124800;
```

✅ **避免 SELECT ***：
```sql
-- ❌ 查询所有字段
SELECT * FROM ttpos_product WHERE status = 1;

-- ✅ 只查询需要的字段
SELECT id, product_name, price FROM ttpos_product WHERE status = 1;
```

### 2. 分页优化

✅ **使用 LIMIT 分页**：
```sql
-- 基础分页
SELECT * FROM ttpos_product 
WHERE status = 1 
ORDER BY create_time DESC 
LIMIT 20 OFFSET 0;

-- 深分页优化（使用子查询）
SELECT * FROM ttpos_product 
WHERE id >= (
    SELECT id FROM ttpos_product 
    WHERE status = 1 
    ORDER BY create_time DESC 
    LIMIT 1 OFFSET 10000
) 
AND status = 1 
ORDER BY create_time DESC 
LIMIT 20;
```

### 3. 批量操作

✅ **批量插入**：
```sql
-- ✅ 批量插入
INSERT INTO ttpos_product (product_name, price) VALUES
('商品1', 100.00),
('商品2', 200.00),
('商品3', 300.00);

-- ❌ 逐条插入
INSERT INTO ttpos_product (product_name, price) VALUES ('商品1', 100.00);
INSERT INTO ttpos_product (product_name, price) VALUES ('商品2', 200.00);
INSERT INTO ttpos_product (product_name, price) VALUES ('商品3', 300.00);
```

### 4. 连接池配置

✅ **合理配置连接池**：
```yaml
database:
  default:
    maxIdleConns: 10      # 最大空闲连接数
    maxOpenConns: 100     # 最大打开连接数
    connMaxLifetime: 3600 # 连接最大生命周期（秒）
```

## 软删除规范

### 1. 软删除实现

✅ **使用 delete_time 字段**：
```sql
-- 软删除
UPDATE ttpos_product SET delete_time = UNIX_TIMESTAMP() WHERE id = 1;

-- 查询未删除的数据
SELECT * FROM ttpos_product WHERE delete_time = 0;

-- 恢复数据
UPDATE ttpos_product SET delete_time = 0 WHERE id = 1;
```

### 2. 唯一索引处理

✅ **软删除时的唯一索引**：
```sql
-- 方案1：复合唯一索引（包含delete_time）
UNIQUE KEY `uk_username_delete` (`username`, `delete_time`)

-- 方案2：删除时修改唯一字段
UPDATE ttpos_user 
SET username = CONCAT(username, '_deleted_', id),
    delete_time = UNIX_TIMESTAMP()
WHERE id = 1;
```

## 数据备份规范

### 1. 定期备份

✅ **备份策略**：
- 每日全量备份
- 每小时增量备份
- 保留30天备份
- 异地备份

### 2. 备份脚本

```bash
#!/bin/bash
# 数据库备份脚本

DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="/backup/mysql"
DB_NAME="shop8267304538112000"

mysqldump -u root -p \
    --single-transaction \
    --routines \
    --triggers \
    --events \
    $DB_NAME > $BACKUP_DIR/${DB_NAME}_${DATE}.sql

# 压缩备份文件
gzip $BACKUP_DIR/${DB_NAME}_${DATE}.sql

# 删除30天前的备份
find $BACKUP_DIR -name "*.sql.gz" -mtime +30 -delete
```

---

**最后更新**: 2025-11-16  
**维护者**: TTPOS Team

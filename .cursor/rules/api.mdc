---
alwaysApply: false
description: API 设计规范
---

# API 设计规范

> 🤖 **受众**: AI Agent  
> 📖 **用途**: API 设计的核心约束和快速参考

---

## 📚 快速索引

| 我要...           | 查阅文档                                                    |
| ----------------- | ----------------------------------------------------------- |
| 学习详细 API 设计 | [API 设计指南](../../docs/human/guides/api-design-guide.md) |
| 快速查看规范      | [API 规范速查](../../docs/shared/api/conventions.md)        |

---

## ⚡ 核心约束（必须遵守）

### URL 命名 - snake_case

```
✅ /api/v1/user_profiles
✅ /api/v1/order_items

❌ /api/v1/userProfiles   # camelCase
❌ /api/v1/user-profiles  # kebab-case
```

### 响应格式

**data 必须是对象，不能是 null 或数组**

```json
{
  "code": 1,
  "message": "success",
  "data": {}
}
```

```json
// ❌ 错误
{"data": null}
{"data": []}

// ✅ 正确
{"data": {}}
{"data": {"list": []}}
```

### 删除接口 - DELETE 方法

**所有删除操作必须使用 DELETE HTTP 方法**

**详细说明和代码示例**：参见 [API 设计指南 - DELETE 方法](../../docs/human/guides/api-design-guide.md#delete-方法---删除接口规范) 章节

### 切片初始化规范

**响应体中的切片必须使用 `make` 初始化，避免返回 null**

**详细代码示例**：参见 [API 设计指南 - 切片初始化规范](../../docs/human/guides/api-design-guide.md#切片初始化规范) 章节

---

## 📋 API 设计规则

### 路由注册（强制，强制，强制）

**每个 API 文件必须包含注册函数，并在 router.go 中调用**

**详细代码示例**：参见 [API 设计指南 - 路由注册](../../docs/human/guides/api-design-guide.md#路由注册) 章节

### URL 设计

```
✅ 使用名词单数 + 操作后缀
GET    /api/v1/order/list          # 获取列表
GET    /api/v1/order/info          # 获取详情
POST   /api/v1/order/create        # 创建
POST   /api/v1/order/update        # 更新
DELETE /api/v1/order/delete        # 删除

❌ 不使用 RESTful 风格
GET  /api/v1/orders
GET  /api/v1/orders/123
```

### HTTP 方法（RESTful API 规范）

**必须遵循以下规则：**

| 方法   | 用途           | 示例                          | 说明                    |
| ------ | -------------- | ----------------------------- | ----------------------- |
| GET    | 获取信息       | `GET /api/v1/order/list`      | 查询、列表、详情        |
| POST   | 创建/修改数据  | `POST /api/v1/order/create`    | 创建、更新、执行操作    |
| DELETE | 删除数据       | `DELETE /api/v1/order/delete` | 删除操作                |
| PUT    | **禁止使用**   | -                             | **项目禁止使用 PUT 方法** |

**规则说明：**

- ✅ **GET 请求**：用于获取信息（查询、列表、详情），参数通过 Query 传递
- ✅ **POST 请求**：用于创建和修改数据，参数通过 Body 传递
- ✅ **DELETE 请求**：用于删除数据，参数可通过 Query 或 Body 传递
- ❌ **PUT 请求**：禁止使用

**详细代码示例**：参见 [API 设计指南 - HTTP 方法](../../docs/human/guides/api-design-guide.md#http-方法) 和 [代码示例](../../docs/human/guides/api-design-guide.md#代码示例) 章节

---

## 🔍 关键检查点

### 创建 API 文件时

- [ ] 必须包含 `Register*Handlers` 函数
- [ ] 在 `router/router.go` 中调用注册函数
- [ ] 注册函数中初始化所有依赖服务
- [ ] 注册函数中创建 Handler 实例
- [ ] 注册函数中注册所有路由

### 设计 API 时

- [ ] URL 使用 snake_case
- [ ] 使用名词单数 + 操作后缀
- [ ] data 必须是对象
- [ ] 包含版本号 /api/v1/
- [ ] **获取信息使用 GET 请求（查询、列表、详情）**
- [ ] **创建和修改数据使用 POST 请求**
- [ ] **删除数据使用 DELETE 请求**
- [ ] **禁止使用 PUT 请求**
- [ ] **GET 请求参数通过 Query 传递，不使用 Body**

### 编写响应时

- [ ] code: 1 表示成功，0 表示失败
- [ ] message: 提示信息
- [ ] data: 必须是对象 `{}`
- [ ] 分页信息放在 meta 中

### 返回数据时

- [ ] Go: 使用 helper.Success()
- [ ] PHP: 使用 renderSuccess()
- [ ] 空数据返回 `{}`，不返回 null
- [ ] **Go 响应体中的切片必须使用 `make` 初始化，避免返回 null**

### 多语言字段（请求和响应）

- [ ] **所有多语言字段（请求参数和响应参数）必须使用 `dto.LocaleResponse` 类型**
- [ ] **多语言字段名称必须使用 `LocaleName` 或 `LocaleXXXName` 格式命名**（如 `LocaleName`、`LocaleAttributeName`、`LocaleUnitName` 等）
- [ ] ❌ **禁止使用** 字符串、map[string]string 或其他自定义结构
- [ ] ❌ **禁止使用** `Name`、`AttributeName` 等不带 `Locale` 前缀的字段名

**详细代码示例**：参见 [API 设计指南 - 多语言字段示例](../../docs/human/guides/api-design-guide.md#多语言字段示例) 章节

---

## 📋 代码示例

**所有详细的代码示例已移至** [API 设计指南 - 代码示例](../../docs/human/guides/api-design-guide.md#代码示例) 章节，包括：

- 路由注册完整示例
- GET/POST/DELETE 请求处理示例
- 响应处理示例（Go/PHP）
- 多语言字段使用示例
- 分页响应格式示例

---

## 🚫 禁止事项

| 禁止                        | 原因                      |
| --------------------------- | ------------------------- |
| ❌ API 文件无注册函数       | 路由无法生效              |
| ❌ 注册函数未在 router 调用 | 路由无法生效              |
| ❌ URL 用 kebab-case        | 项目统一用 snake_case     |
| ❌ data 返回 null           | 前端无法统一处理          |
| ❌ data 返回数组            | 必须是对象                |
| ❌ 使用 RESTful 风格        | 项目用操作后缀            |
| ❌ 直接用 c.JSON            | 必须用 helper             |
| ❌ **获取信息使用 POST 请求** | **必须使用 GET 请求** |
| ❌ **删除数据使用 POST 请求** | **必须使用 DELETE 请求** |
| ❌ **使用 PUT 请求** | **项目禁止使用 PUT 方法** |
| ❌ **GET 请求使用 Body 传递参数** | **GET 请求参数必须通过 Query 传递** |
| ❌ **多语言字段（请求参数和响应参数）使用 map[string]string、字符串或自定义结构** | **必须用 dto.LocaleResponse** |
| ❌ **多语言字段名不是 LocaleName 或 LocaleXXXName 格式** | **必须使用 LocaleName 或 LocaleXXXName 格式** |
| ❌ **响应体中的切片未使用 make 初始化** | **必须使用 make 初始化，避免返回 null** |

---

## 📖 详细文档

- [API 设计详细指南](../../docs/human/guides/api-design-guide.md) - 完整的 API 设计规范
- [API 规范速查](../../docs/shared/api/conventions.md) - 快速参考
- [Go Main 开发指南](../../docs/human/guides/go-main-development.md) - Go API 实现
- [PHP 开发指南](../../docs/human/guides/php-development.md) - PHP API 实现

---

**最后更新**: 2025-11-20  
**维护者**: TTPOS Team

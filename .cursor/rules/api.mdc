---
alwaysApply: false
description: API 设计规范
---

# API 设计规范

> 🤖 **受众**: AI Agent  
> 📖 **用途**: API 设计的核心约束和快速参考

---

## 📚 快速索引

| 我要...           | 查阅文档                                                    |
| ----------------- | ----------------------------------------------------------- |
| 学习详细 API 设计 | [API 设计指南](../../docs/human/guides/api-design-guide.md) |
| 快速查看规范      | [API 规范速查](../../docs/shared/api/conventions.md)        |

---

## ⚡ 核心约束（必须遵守）

### URL 命名 - snake_case

```
✅ /api/v1/user_profiles
✅ /api/v1/order_items

❌ /api/v1/userProfiles   # camelCase
❌ /api/v1/user-profiles  # kebab-case
```

### 响应格式

**data 必须是对象，不能是 null 或数组**

```json
{
  "code": 1,
  "message": "success",
  "data": {}
}
```

```json
// ❌ 错误
{"data": null}
{"data": []}

// ✅ 正确
{"data": {}}
{"data": {"list": []}}
```

---

## 📋 API 设计规则

### 路由注册（强制，强制，强制）

**每个 API 文件必须包含注册函数**

```go
// ✅ 正确：包含注册函数
func Register*Handlers(router gin.IRouter, dbm *database.DBManager, cache cache.Cache) {
    // 1. 初始化所有依赖服务
    captchaSrv := service.NewCaptchaSrv(cache)
    settingSrv := setting.NewSrv(dbm, cache)
    authSrv := service.NewAuthSrv(...)
    
    // 2. 创建 Handler 实例
    handler := &ExampleHandler{
        exampleSrv: service.NewExampleSrv(dbm),
    }
    
    // 3. 注册路由
    // middleware.Auth(authSrv, dbm) (强制)
    privateApi := router.Group("", middleware.Auth(authSrv, dbm))
    {
        privateApi.GET("/example/list", handler.GetList)
        privateApi.POST("/example/create", handler.Create)
    }
}

// ❌ 错误：没有注册函数
// API 文件必须有 Register*Handlers 函数
```

**注册函数必须在 router.go 中调用**

```go
// router/router.go
shopGroup := apiV1.Group("/shop")
{
    shop.RegisterExampleHandlers(shopGroup, dbm, cache)  // ✅ 必须调用
}
```

### URL 设计

```
✅ 使用名词单数 + 操作后缀
GET  /api/v1/order/list
GET  /api/v1/order/info
POST /api/v1/order/create
POST /api/v1/order/cancel

❌ 不使用 RESTful 风格
GET  /api/v1/orders
GET  /api/v1/orders/123
```

### HTTP 方法

| 方法   | 用途      | 示例                          |
| ------ | --------- | ----------------------------- |
| GET    | 获取资源  | `GET /api/v1/order/list`      |
| POST   | 创建/执行 | `POST /api/v1/order/create`   |
| PUT    | 完整更新  | `PUT /api/v1/order/update`    |
| DELETE | 删除      | `DELETE /api/v1/order/delete` |

---

## 🔍 关键检查点

### 创建 API 文件时

- [ ] 必须包含 `Register*Handlers` 函数
- [ ] 在 `router/router.go` 中调用注册函数
- [ ] 注册函数中初始化所有依赖服务
- [ ] 注册函数中创建 Handler 实例
- [ ] 注册函数中注册所有路由

### 设计 API 时

- [ ] URL 使用 snake_case
- [ ] 使用名词单数 + 操作后缀
- [ ] data 必须是对象
- [ ] 包含版本号 /api/v1/

### 编写响应时

- [ ] code: 1 表示成功，0 表示失败
- [ ] message: 提示信息
- [ ] data: 必须是对象 `{}`
- [ ] 分页信息放在 meta 中

### 返回数据时

- [ ] Go: 使用 helper.Success()
- [ ] PHP: 使用 renderSuccess()
- [ ] 空数据返回 `{}`，不返回 null

### 多语言字段

- [ ] 由前端添加上来的多语言字段，统一使用 `dto.LocaleResponse` 类型
- [ ] 多语言字段名称统一使用 `LocaleName`（参考 `main/app/dto/req/product.go`）

```go
// ✅ 正确
type ExampleReq struct {
    LocaleName dto.LocaleResponse `json:"locale_name" binding:"required"` // 名称（多语言）
}

// ❌ 错误
type ExampleReq struct {
    Name map[string]string `json:"name"` // 不要使用 map[string]string
    Names map[string]string `json:"names"` // 不要使用 names
}
```

---

## 📋 常用模式

### Go 返回响应

```go
// ✅ 正确
helper.Success(c, gin.H{})
helper.Success(c, gin.H{"list": []})
helper.ErrorWithDetail(c, constant.CodeFail, err)

// ❌ 错误
c.JSON(200, data)  // 不要直接用 c.JSON
```

### PHP 返回响应

```php
// ✅ 正确
return $this->renderSuccess($data);
return $this->renderSuccess([]);
return $this->renderError('错误信息');
```

### 分页响应

```json
{
  "code": 1,
  "message": "success",
  "data": {
    "list": [...],
    "meta": {
      "page_no": 1,
      "page_size": 20,
      "total": 100
    }
  }
}
```

### 多语言字段

```go
// DTO 定义
type ProductReq struct {
    LocaleName dto.LocaleResponse `json:"locale_name" binding:"required"` // 商品名称（多语言）
    Price      float64             `json:"price"`                          // 价格
}

// 前端提交的 JSON
{
  "locale_name": {
    "zh": "商品名称",
    "th": "ชื่อสินค้า",
    "en": "Product Name",
    "zhtw": "商品名稱",
    "ja": "商品名",
    "ko": "상품명",
    "my": "ကုန်ပစ္စည်းအမည်",
    "tr": "Ürün Adı",
    "sv": "Produktnamn"
  },
  "price": 100.00
}
```

**返回数据时**

```go
// 响应 DTO
type ProductResp struct {
    Uuid       uint64             `json:"uuid"`        // 商品UUID
    LocaleName dto.LocaleResponse `json:"locale_name"` // 商品名称（多语言）
    Price      float64            `json:"price"`       // 价格
}

// 从 Model 转换
resp := ProductResp{
    Uuid:       product.Uuid,
    LocaleName: product.MultiLanguageName.GetLocaleResponse(), // 从数据库获取
    Price:      product.Price,
}
```

---

## 🚫 禁止事项

| 禁止                        | 原因                      |
| --------------------------- | ------------------------- |
| ❌ API 文件无注册函数       | 路由无法生效              |
| ❌ 注册函数未在 router 调用 | 路由无法生效              |
| ❌ URL 用 kebab-case        | 项目统一用 snake_case     |
| ❌ data 返回 null           | 前端无法统一处理          |
| ❌ data 返回数组            | 必须是对象                |
| ❌ 使用 RESTful 风格        | 项目用操作后缀            |
| ❌ 直接用 c.JSON            | 必须用 helper             |
| ❌ 多语言用 map[string]string | 必须用 dto.LocaleResponse |
| ❌ 多语言字段名不是 LocaleName | 统一使用 LocaleName       |

---

## 📖 详细文档

- [API 设计详细指南](../../docs/human/guides/api-design-guide.md) - 完整的 API 设计规范
- [API 规范速查](../../docs/shared/api/conventions.md) - 快速参考
- [Go Main 开发指南](../../docs/human/guides/go-main-development.md) - Go API 实现
- [PHP 开发指南](../../docs/human/guides/php-development.md) - PHP API 实现

---

**最后更新**: 2025-11-20  
**维护者**: TTPOS Team

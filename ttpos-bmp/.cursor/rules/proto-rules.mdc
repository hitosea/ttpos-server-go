---
alwaysApply: true
---

# Protobuf 开发规范

## 初始化
- 当环境缺少 protocol依赖时，安装编译的依赖 
  ```bash
  # 安装 protocol 编译器插件
  go install google.golang.org/protobuf/cmd/protoc-gen-go@latest
  go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@latest
  # 更新 PATH 以便 protoc 能找到插件
  export PATH="$PATH:$(go env GOPATH)/bin"
  # 验证安装
  protoc --version
  protoc-gen-go --version
  protoc-gen-go-grpc --version
  ```

## 1. 文件命名规范

### 1.1 文件命名
- 文件名使用小写字母，单词间用下划线分隔
- 例如：`user_service.proto`, `order_management.proto`

### 1.2 包命名
- 包名使用小写字母，与文件名保持一致
- 例如：`package user_service;`

## 2. 消息命名规范

### 2.1 请求消息命名
- **必须**以 `Req` 结尾
- 使用大驼峰命名法（PascalCase）
- 格式：`{ServiceName}{Action}Req`
- 例如：`GetUserInfoReq`, `CreateOrderReq`, `UpdateCompanyReq`

### 2.2 响应消息命名
- **必须**以 `Resp` 结尾
- 使用大驼峰命名法（PascalCase）
- 格式：`{ServiceName}{Action}Resp`
- 例如：`GetUserInfoResp`, `CreateOrderResp`, `UpdateCompanyResp`

### 2.3 其他消息命名
- 内部消息使用描述性名称
- 例如：`UserInfo`, `OrderDetails`, `CompanyData`

## 3. 服务定义规范

### 3.1 服务命名
- 服务名以 `Service` 结尾
- 使用大驼峰命名法（PascalCase）
- 例如：`UserService`, `OrderService`, `CompanyService`

### 3.2 方法命名
- 方法名使用大驼峰命名法（PascalCase）
- 使用动词+名词的形式
- 例如：`GetUserInfo`, `CreateOrder`, `UpdateCompany`

## 4. 字段定义规范

### 4.1 字段命名
- 字段名使用小写字母，单词间用下划线分隔（snake_case）
- 例如：`user_id`, `company_name`, `create_time`

### 4.2 字段类型选择
- **字符串**：`string` - 用于文本数据
- **数字**：`int32`, `int64`, `uint32`, `uint64` - 根据数值范围选择
- **浮点数**：`float`, `double` - 根据精度要求选择
- **布尔值**：`bool` - 用于逻辑值
- **字节数组**：`bytes` - 用于二进制数据
- **时间戳**：`google.protobuf.Timestamp` - 用于时间数据

### 4.3 字段编号
- 字段编号从 1 开始，按顺序递增
- 避免使用 19000-19999 的编号（Google 保留）
- 为未来扩展预留编号空间

## 5. 注释规范

### 5.1 文件注释
```protobuf
// 服务名称 Protobuf 定义文件
// 用于定义服务相关的 gRPC 服务接口和消息结构
syntax = "proto3";
```

### 5.2 服务注释
```protobuf
// 服务名称服务接口定义
// 提供服务相关的 gRPC 服务方法
service ServiceName {
  // 方法功能描述
  // 参数说明
  // 返回值说明
  rpc MethodName(RequestMessage) returns (ResponseMessage);
}
```

### 5.3 消息注释
```protobuf
// 请求/响应消息描述
// 详细说明消息的用途和包含的数据
message MessageName {
  string field_name = 1; // 字段描述，说明字段的用途和格式
}
```

## 6. 导入规范

### 6.1 标准库导入
```protobuf
import "google/protobuf/timestamp.proto";
import "google/protobuf/empty.proto";
```

### 6.2 项目内导入
```protobuf
import "common/common.proto";
import "user/user.proto";
```

## 7. 完整示例

```protobuf
// 用户服务 Protobuf 定义文件
// 用于定义用户相关的 gRPC 服务接口和消息结构
syntax = "proto3";

package user_service;

// Go 包路径配置
option go_package = "ttpos-bmp/app/ttpos-erp/internal/service/user";

// 导入标准库
import "google/protobuf/timestamp.proto";

// 用户服务接口定义
// 提供用户信息管理相关的 gRPC 服务方法
service UserService {
  // 根据用户ID获取用户信息
  // 参数：用户ID
  // 返回：用户详细信息
  rpc GetUserInfo(GetUserInfoReq) returns (GetUserInfoResp);
  
  // 创建新用户
  // 参数：用户基本信息
  // 返回：创建结果和用户ID
  rpc CreateUser(CreateUserReq) returns (CreateUserResp);
  
  // 更新用户信息
  // 参数：用户ID和更新数据
  // 返回：更新结果
  rpc UpdateUser(UpdateUserReq) returns (UpdateUserResp);
}

// 获取用户信息请求消息
// 用于向服务端发送获取用户信息的请求
message GetUserInfoReq {
  uint64 user_id = 1; // 用户ID，必填参数
}

// 获取用户信息响应消息
// 服务端返回的用户详细信息
message GetUserInfoResp {
  uint64 user_id = 1;                    // 用户ID
  string username = 2;                   // 用户名
  string email = 3;                      // 邮箱地址
  string phone = 4;                      // 手机号码
  bool is_active = 5;                    // 是否激活
  google.protobuf.Timestamp create_time = 6; // 创建时间
  google.protobuf.Timestamp update_time = 7; // 更新时间
}

// 创建用户请求消息
message CreateUserReq {
  string username = 1; // 用户名，必填
  string email = 2;    // 邮箱地址，必填
  string phone = 3;    // 手机号码，可选
  string password = 4; // 密码，必填
}

// 创建用户响应消息
message CreateUserResp {
  uint64 user_id = 1;  // 新创建的用户ID
  bool success = 2;    // 创建是否成功
  string message = 3;  // 结果消息
}

// 更新用户请求消息
message UpdateUserReq {
  uint64 user_id = 1;  // 用户ID，必填
  string email = 2;    // 新邮箱地址，可选
  string phone = 3;    // 新手机号码，可选
  bool is_active = 4;  // 激活状态，可选
}

// 更新用户响应消息
message UpdateUserResp {
  bool success = 1;    // 更新是否成功
  string message = 2;  // 结果消息
}
```

## 8. 最佳实践

### 8.1 版本兼容性
- 不要删除或修改已存在的字段编号
- 新增字段使用新的编号
- 使用 `reserved` 关键字标记已废弃的字段

### 8.2 性能优化
- 合理使用 `repeated` 字段
- 避免过深的嵌套结构
- 考虑使用 `oneof` 处理可选字段

### 8.3 安全性
- 敏感信息不要直接暴露在 protobuf 中
- 使用适当的字段类型和验证

### 8.4 文档维护
- 及时更新注释和文档
- 保持消息结构的一致性
- 定期审查和重构过时的定义

## 9. 项目集成规范

### 9.1 与 GoFrame 集成
- protobuf 文件放在 `manifest/protobuf/` 目录下
- 生成的 Go 代码放在 `api/` 目录下
- gRPC 服务注册在 `internal/controller/rpc/` 目录下

### 9.2 代码生成
- 使用 GoFrame 的 `gf gen proto` 命令生成代码
- 生成的代码不要手动修改
- 通过修改 protobuf 文件来更新接口定义


# BMP Integration test environment defaults
# Used by docker-compose.yml and CI pipeline
# Override any variable by exporting it before running make test-bmp-local

# MySQL
DB_ROOT_PASSWORD=testroot
DB_USERNAME=test
DB_PASSWORD=test

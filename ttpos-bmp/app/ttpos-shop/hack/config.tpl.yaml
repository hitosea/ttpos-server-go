
# CLI tool, only in development environment.
# https://goframe.org/docs/cli
gfcli:
  gen:
    pbentity:
        link: "mysql:$DB_USERNAME:$DB_PASSWORD@tcp($DB_HOST:$DB_PORT_OPEN)/saas"
        path: "manifest/protobuf/shop"
        removePrefix: "ttpos_"
        #tables: "order,products"
        package: "ttpos-bmp/app/ttpos-shop/api/rpc/shop"
    pb:
        api: "api/rpc"
        ctrl: "internal/controller"
    dao:
      - link: "mysql:$DB_USERNAME:$DB_PASSWORD@tcp($DB_HOST:$DB_PORT_OPEN)/saas"
        descriptionTag: true
        removePrefix: "ttpos_"

  run:
    path: "./bin"
    extra: ""
    args: "all"
    watchPaths:
      - api/*.go
      - internal/*.go
  docker:
    build: "-a amd64 -s linux -p temp -ew"
    tagPrefixes:
      - hub.hitosea.com/ttpos-bmp-shop
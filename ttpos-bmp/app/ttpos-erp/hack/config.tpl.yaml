
# CLI tool, only in development environment.
# https://goframe.org/docs/cli
gfcli:
  gen:
    pbentity:
      link: "mysql:$DB_USERNAME:$DB_PASSWORD@tcp($DB_HOST:$DB_PORT_OPEN)/erp"
      path: "manifest/protobuf/pbentity"
      removePrefix: "erp_"
      #tables: "order,products"
      package: "ttpos-bmp/app/ttpos-erp/api/pbentity"
    pb:
      api: "api"
      ctrl: "internal/controller/rpc"
    dao:
      - link: "mysql:$DB_USERNAME:$DB_PASSWORD@tcp($DB_HOST:$DB_PORT_OPEN)/erp"
        descriptionTag: true
        removePrefix: "erp_"
        tablesEx: "schema_migrations"

  run:
    path: "./bin"
    extra: ""
    args: ""
    watchPaths:
      - api/*.go
      - internal/*.go
  build:
    path: "./bin"
    system: linux
    arch: amd64
  docker:
    build: "-a amd64 -s linux -p temp -ew"
    tagPrefixes:
      - hub.hitosea.com/ttpos-bmp-erp

#注意db的格式跟link的不一样 用容器内网ip 端口
migrate-db-link: mysql://$DB_USERNAME:$DB_PASSWORD@tcp($DB_HOST:$DB_PORT)/erp
migrate-db-link-open: mysql://$DB_USERNAME:$DB_PASSWORD@tcp($DB_HOST:$DB_PORT_OPEN)/erp
